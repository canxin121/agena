# AGENTS.md

本文件定义本项目（`Agena`）的工程原则与执行规范。

## 1. 项目目标

- 当前阶段仅实现**后端核心**（不做前端、不做 TUI）。
- 面向多 AI Provider（OpenAI / Claude / Gemini 等）构建高性能、高可维护的 Rust Agent 后端。
- 架构优先：先稳定领域模型、协议和扩展点，再迭代功能。

## 2. Rust 设计原则

- 遵循 Rust 范式：优先 `struct` / `enum` / `trait` / `impl` 组合建模。
- 优先类型系统表达约束，减少“注释驱动逻辑”。
- 能使用 `From` / `TryFrom` / `Display` / `Error` 等标准 trait 的场景，不手搓重复函数。
- 内部传递消息时，避免以 `str` / `String` 作为“业务键”；优先使用枚举、newtype、强类型 ID。

## 3. 协议与序列化

- 内部协议采用强类型（`enum` + tagged `serde`）。
- 与外部系统（Provider API、未来前端/服务）通信统一走 `serde` 序列化/反序列化。
- 禁止手写 ad-hoc 字符串拼接协议或自定义脆弱解析逻辑。

## 4. Provider 接入策略

- 优先使用官方 SDK；若无官方 SDK，优先选择社区高质量第三方库。
- 仅在“不存在合适实现”或“性能/可控性不足”时，才自行实现 HTTP 适配层。
- 所有 Provider 通过统一 trait 接口抽象，禁止把某家 Provider 特性散落到核心业务层。

## 5. 错误处理与可观测性

- 错误统一使用 `thiserror` 建模，分层语义明确（配置、协议、Provider、存储、运行时）。
- 日志与追踪统一使用 `tracing`，避免无结构化 `println!`。

## 6. 存储与并发

- 异步运行时统一使用 `tokio`。
- ORM 层使用 `sea-orm`，避免业务层直写散乱 SQL。
- 并发设计优先无共享可变状态；必要共享使用明确边界与最小锁粒度。

## 7. 代码组织与演进

- 保持模块职责清晰：`provider`、`message`、`agent`、`config`、`db`、`error` 等边界明确。
- 禁止巨型 God file；单文件过大时优先拆分子模块。
- 新增功能必须附带最小可验证路径（示例、测试或 smoke 检查）。

## 8. 实施约束（当前阶段）

- 先完成后端核心能力与模块稳定性，再接入 UI 层。
- 优先保证正确性、类型安全和可维护性，其次优化局部性能。
- 每次变更需要说明：**改了什么、为什么改、如何验证**。
