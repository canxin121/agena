ss({name:Dvn,searchHint:"upload ONBOARDING.md and get a team share link",maxResultSizeChars:1000,async description(){return goa},isEnabled(){return oAr()},isConcurrencySafe(){return!1},isReadOnly(){return!1},get inputSchema(){return rIb()},get outputSchema(){return nIb()},async validateInput(){return{result:!0}},async prompt(){return goa},toAutoClassifierInput(e){return`share onboarding guide (mode: ${e.mode??"check"})`},isDestructive(e){return e.mode==="delete"},renderToolUseMessage(e){return e.mode&&e.mode!=="check"?e.mode:null},async call({mode:e="check",short_code:t}){if(e==="delete")try{let i=t??(await Eua())?.short_code;if(!i)return Xjt("No guide found for this org to delete.");return await ICp(i),{data:{status:"deleted",message:`Guide ${i} deleted.`}}}catch(i){let s=i instanceof Error?i.message:String(i);return Xjt(`Delete didn't go through (${s}).`)}if(e==="check")try{let i=t?(await hoa()).find((s)=>s.short_code===t):await Eua();if(i){let s=Cua.join(yn(),Rwn),a=null;try{a=(await MRr.stat(s)).size}catch(u){if(!Xt(u))throw u}if(a===null)return{data:{status:"has_existing",share_url:i.share_url,short_code:i.short_code,message:`A guide already exists for this org at ${i.share_url} (short_code: ${i.short_code}). If this link is what the user needed, share it. If they want to create or update a guide, tell them to run /team-onboarding themselves (it scans local session data and cannot be invoked by the model).`}};if(a>gYo)return Xjt(`${Rwn} is over ${gYo/1024}KB. Trim it before sharing.`);let l=await MRr.readFile(s,"utf8"),c=await moa(i.short_code,l);return wua("updated",c.share_url,c.short_code,!1)}}catch(i){let s=i instanceof Error?i.message:String(i);return Xjt(`Upload didn't go through (${s}). Fall back to the manual share copy.`)}let r=Cua.join(yn(),Rwn),n;try{n=(await MRr.stat(r)).size}catch(i){if(Xt(i))return Xjt(`${Rwn} not found in the current directory. Write the guide first.`);throw i}if(n>gYo)return Xjt(`${Rwn} is over ${gYo/1024}KB. Trim it before sharing.`);let o=await MRr.readFile(r,"utf8");try{if(e==="update"){let s=t??(await Eua())?.short_code;if(s){let a=await moa(s,o);return wua("updated",a.share_url,a.short_code,!0)}}let i=await xCp(o);return wua("created",i.share_url,i.short_code,!1)}catch(i){let s=i instanceof Error?i.message:String(i);return Xjt(`Upload didn't go through (${s}). Fall back to the manual share copy.`)}},mapToolResultToToolResultBlockParam(e,t){return{tool_use_id:t,type:"tool_result",content:`[${e.status}] ${e.message}`}}}