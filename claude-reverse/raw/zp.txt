()),s=FSt.join(i,"..","..");r=FSt.join(s,"..")}}catch{}if(!await $Ue(e)){C(`[TeammateTool] kept worktree \u2014 unremovable reparse point in ${e}`);return}if(r){let n=await Jn(so(),["worktree","remove","--force",e],{cwd:r});if(n.code===0){C(`[TeammateTool] Removed worktree via git: ${e}`);return}if(n.stderr?.includes("not a working tree")){C(`[TeammateTool] Worktree already removed: ${e}`);return}C(`[TeammateTool] git worktree remove failed, falling back to rm: ${n.stderr}`)}try{await W6e.rm(e,{recursive:!0,force:!0}),C(`[TeammateTool] Removed worktree directory manually: ${e}`)}catch(n){C(`[TeammateTool] Failed to remove worktree ${e}: ${ue(n)}`)}}function ita(e){nVn().add(e)}async function Elb(){return Ou("swarm_session_cleanup",async()=>{let e=nVn();if(e.size===0)return;let t=Array.from(e);C(`cleanupSessionTeams: removing ${t.length} orphan team dir(s): ${t.join(", ")}`),await Promise.allSettled(t.map((r)=>wlb(r))),await Promise.allSettled(t.map((r)=>U_p(r))),e.clear()})}async function wlb(e){let t=await eM(e);if(!t)return;let r=t.members.filter((a)=>a.name!==Um&&a.tmuxPaneId&&a.backendType&&jgn(a.backendType));if(r.length===0)return;let[{ensureBackendsRegistered:n,getBackendByType:o},{isInsideTmux:i}]=await Promise.all([Promise.resolve().then(() => (gbt(),N6d)),Promise.resolve().then(() => (xKe(),ILu))]);await n();let s=!await i();await Promise.allSettled(r.map(async(a)=>{if(!a.tmuxPaneId||!a.backendType||!jgn(a.backendType))return;let l=await o(a.backendType).killPane(a.tmuxPaneId,s);C(`cleanupSessionTeams: killPane ${a.name} (${a.backendType} ${a.tmuxPaneId}) \u2192 ${l}`)}))}async function U_p(e){return Ou("swarm_team_cleanup",async()=>{let t=await eM(e),r=[];if(t){for(let o of t.members)if(o.worktreePath)r.push(o.worktreePath)}for(let o of r)await vlb(o);let n=rGo(e);try{await W6e.rm(n,{recursive:!0,force:!0}),C(`[TeammateTool] Cleaned up team directory: ${n}`)}catch(o){C(`[TeammateTool] Failed to clean up team directory ${n}: ${ue(o)}`)}})}var W6e,FSt,Slb;var d6=v(()=>{ft();Kt();bd();We();Zr();lt();Cl();el();zN();Hr();SL();c4s();rr();Lm();_nn();$vr();NG();W6e=require("fs/promises"),FSt=require("path");Slb={realpath:!1,retries:{retries:10,minTimeout:5,maxTimeout:100},onCompromised:()=>{}}});function j_p(e,t){for(let r of Object.values(t.tasks))if(S0(r)&&r.identity.agentName===e)return r.id;return}function sta(e,t,r){t.update(e,(n)=>({...n,awaitingPlanApproval:r}))}function W_p(e,t,r){let n=r.get(e);if(!n||!S0(n)||!n.awaitingPlanApproval)return!1;if(!t.approved)return sta(e,r,!1),!0;let o=ata(t.permissionMode);return r.update(e,(i)=>({...i,awaitingPlanApproval:!1,permissionMode:o})),y0r(n.identity.teamName,n.identity.agentName,o),!0}function ata(e){let t=EL(OV(e??"default"));if(t==="bypassPermissions"&&YL())return"default";if(t==="auto"&&!pI())return"default";return t}var lta=v(()=>{CKe();kE();LC();d6()});var cta;var G_p=v(()=>{cta=class cta extends Error{constructor(e){super(e);this.name="PlanPreconditionError"}}});function _Tn(e){if(!e||SG()!=="default")return"";return`

If this plan can be broken down into multiple independent tasks, consider spawning named teammates with the ${ti} tool (pass a \`name\`) to parallelize the work.`}var z_p;var uta=v(()=>{Hve();zh();kj();z_p=`Use this tool when you are in plan mode and have finished writing your plan to the plan file and are ready for user approval.

## How This Tool Works
- You should have already written your plan to the plan file specified in the plan mode system message
- This tool does NOT take the plan content as a parameter - it will read the plan from the file you wrote
- This tool simply signals that you're done planning and ready for the user to review and approve
- The user will see the contents of your plan file when they review it

## When to Use This Tool
IMPORTANT: Only use this tool when the task requires planning the implementation steps of a task that requires writing code. For research tasks where you're gathering information, searching files, reading files or in general trying to understand the codebase - do NOT use this tool.

## Before Using This Tool
Ensure your plan is complete and unambiguous:
- If you have unresolved questions about requirements or approach, use ${om} first (in earlier phases)
- Once your plan is finalized, use THIS tool to request approval

**Important:** Do NOT use ${om} to ask "Is this plan okay?" or "Should I proceed?" - that's exactly what THIS tool does. ExitPlanMode inherently requests user approval of your plan.

## Examples

1. Initial task: "Search for and understand the implementation of vim mode in the codebase" - Do not use the exit plan mode tool because you are not planning the implementation steps of a task.
2. Initial task: "Help me implement yank mode for vim" - Use the exit plan mode tool after you have finished planning the implementation steps of the task.
3. Initial task: "Add a new feature to handle user authentication" - If unsure about auth method (OAuth, JWT, etc.), use ${om} first, then use exit plan mode tool after clarifying the approach.
`});var Clb,V_p,X0A,Alb,r5;var S0r=v(()=>{jn();ft();xt();vS();Fs();Kk();mc();We();lta();epn();An();DR();rr();Lm();Q9();DE();zh();G_p();uta();Clb=Te(()=>E.object({tool:E.enum(["Bash"]).describe("The tool this prompt applies to"),prompt:E.string().describe('Semantic description of the action, e.g. "run tests", "install dependencies"')})),V_p=Te(()=>E.strictObject({allowedPrompts:E.array(Clb()).optional().describe("Deprecated: no longer used.")}).passthrough()),X0A=Te(()=>V_p().extend({plan:E.string().optional().describe("The plan content (injected by normalizeToolInput from disk)"),planFilePath:E.string().optional().describe("The plan file path (injected by normalizeToolInput)")})),Alb=Te(()=>E.object({plan:E.string().nullable().describe("The plan that was presented to the user"),isAgent:E.boolean(),filePath:E.string().optional().describe("The file path where the plan was saved"),hasTaskTool:E.boolean().optional().describe("Whether the Agent tool is available in the current context"),planWasEdited:E.boolean().optional().describe("True when the user edited the plan (CCR web UI or Ctrl+G); determines whether the plan is echoed back in tool_result"),awaitingLeaderApproval:E.boolean().optional().describe("When true, the teammate has sent a plan approval request to the team leader"),requestId:E.string().optional().describe("