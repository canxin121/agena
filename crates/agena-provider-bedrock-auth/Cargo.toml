[package]
name = "agena-provider-bedrock-auth"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "AWS credential-chain leaf adapter for Amazon Bedrock"

[dependencies]
agena-failure = { workspace = true }
aws-credential-types = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
[target.'cfg(not(any(all(target_arch = "aarch64", target_endian = "big"), all(target_arch = "x86", target_os = "windows", target_env = "gnu"))))'.dependencies]
aws-config = { workspace = true, default-features = true }
