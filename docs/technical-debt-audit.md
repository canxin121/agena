# Agena 技术债与可靠性审查记录

审查开始于 2026-09-05，最近一次 Rust 验证完成于 2026-09-07 01:09，文件读取实际 HTTP 验证完成于 2026-09-06 10:29，Web 验证完成于 2026-09-06 10:09（Asia/Singapore），基线提交为 `239ff54d`。范围保持为整个 Agena，基线包含：
64 个 workspace 成员、1,196 个第一方 Rust 源文件、Web 客户端、仓库工具、
构建与发布流程，以及 vendored 依赖。本文记录已验证的发现和后续审查范围；
本轮修复不代表全面审查已经完成。按用户要求，完成最后一轮验证和记录后停止继续展开；下文未完成范围保留供后续决定。

## 已确认并修复

| 问题 | 影响与证据 | 修复与验证入口 |
| --- | --- | --- |
| macOS 禁用 compact unwind | 独立 `catch_unwind` 程序在仓库链接参数下以 SIGABRT 退出，去掉该参数后正常捕获；原工作区测试有 5 个目标直接异常终止 | 删除链接参数，恢复 macOS 运行时能力测试；新增实际插件 transport 的同步/异步 panic 隔离测试 |
| SSE 跨分块 CRLF 解析 | CR 和 LF 分属不同网络读取时产生额外空行，导致多行 JSON 丢失、事件 ID 丢失 | 保存跨读取的 CR 状态；逐字节 UTF-8/CRLF 回归测试 |
| SSE 快照合并保留过期字段 | 后续完整 part 快照已省略的 `summary`、`presentation` 仍被保留 | 完整快照替换；回归验证字段删除 |
| SSE 丢失信号和事件顺序 | 不同运行信号按 session 合并而被吞掉；交错 part 更新会让游标顺序变为 3、2；更新可跨越删除通知 | 保留临时信号，合并后的快照放到实际到达位置，生命周期事件成为合并边界 |
| SSE 丢失收藏/置顶和部分 payload | metadata 转换遗漏 `favorite`、`pinned`，runtime payload 仅接受对象 | 保留布尔值的 true/false 与 JSON payload；回归覆盖数组、字符串 |
| 会话 URL 解析错误 | `#fragment` 被算进 session ID；重复参数与 Vue Router 的解析不一致 | 去掉片段后解析，使用 `getAll`；覆盖编码 `#` 和空值后的有效 ID |
| 删除的 OAuth 地址返回 200 | `/oauth/register` 落到 SPA 首页或 API-only 首页，表现为成功 | 集中保留 API/auth/OAuth/well-known 命名空间，未知地址返回 404；覆盖有/无 Web 构建两种模式 |
| Web 测试契约过期 | 基线 462 项中 14 项失败，仍使用被移除的 model 字段、设备字段、路由、模型键和问题 ID | 对照当前 Rust 类型和客户端代码同步测试；保留旧 SSE envelope 被拒绝的断言 |
| Rust 测试编译与 fixture 失效 | MCP 缺少 Mutex 导入，workflow fixture 使用已删除字段，health fixture 缺少 server identity，工具清单未同步版本/Skill 文案 | 修复编译和 fixture；通过生成器重建并审阅契约差异，工具总数仍为 141 |
| 真实进程测试依赖个人环境 | MCP/restart 测试读取开发者的个人配置；重启测试依赖个人默认模型 | 子进程隔离配置/凭据/代理环境，fixture 明确配置默认模型，不修改个人配置 |
| Anthropic 测试随机连接重置 | HTTP fixture 写完响应后带着未读请求关闭 TCP，macOS 出现 connection reset | 先读取完整 GET 请求头，再返回响应 |
| MCP 测试仍使用旧协议 | 普通调用缺失当前 stateless 协议元数据和标准 HTTP headers；仍期望旧 batch/错误状态行为 | 请求 helper 使用 2026-07-28 契约；验证缺元数据、旧 batch、隐藏工具仍被拒绝 |
| Web 依赖漏洞 | 原锁文件审计为 54 条记录：15 high、33 moderate、6 low | 升级受影响直接/传递依赖，锁定安全 DOMPurify override；Bun 1.3.14 审计为 0 |
| 构建隐式下载并修改源码 | `cargo test --locked` 实际改写 `spider_fingerprint/chrome_versions.rs.fallback` | 默认复制受版本控制的语料到 OUT_DIR；在线刷新改成显式维护操作，并修正 Cargo 文件监听路径 |
| 自动验证与维护脚本缺口 | 主工程没有普通 PR 的 Rust/Web 测试门禁；依赖脚本访问不存在的 web-ui/VS Code 包 | 新增 quality workflow，修复依赖脚本，并提供 development.md 中的本地命令 |
| 遗留无效代码 | 批量文件替换保留两个空分支和只为计数保存的重复路径列表 | 删除空分支，直接使用已完成文件列表计数；Clippy 覆盖 |
| dist 构建不保留插件 panic 隔离 | 同一个插件 transport 可执行用例在原 `dist` 下首次 panic 即以 134 退出；Cargo 的测试 harness 强制 unwind，普通测试无法发现 | dist 改用 unwind；共用行为用例供集成测试和真实 dist 可执行程序使用，加入 Linux/macOS CI |
| Rust LRU 漏洞例外可消除 | vendored Tantivy 使用 lru 0.16.4，而实际调用的缓存 API 与修复版兼容 | 两份 Tantivy manifest 和 workspace 最低版本统一为 0.18.2，删除 RUSTSEC-2026-0253 例外，记录 vendored patch |
| ChaCha20 使用被撤回版本 | 0.10.1 的 RNG SSE2 后端误用 SSE4.1 指令，影响缺少该指令的 x86 CPU | 锁文件升级至 0.10.2；保留其他依赖版本，未在 ARM 开发机上声称已复现 x86 指令异常 |
| RSA 审计例外说明错误 | `cargo tree -i rsa -e features` 证明 jsonwebtoken 的 rust_crypto 后端也引入 RSA，并非仅存在于未启用的 MySQL 路径 | 核对 MCP 只签发/接受 EdDSA，修正例外理由与后续重新评估条件；不把仍有例外的审计结果描述成零风险 |
| 内存调度存储破坏重试和占用契约 | 把 `pending_delivery` 当作执行锁阻止重试；忽略完成凭据与调度时间令牌，旧请求可覆盖新状态 | 独立保存当前占用与比较令牌；内存/SQLite 共用行为回归覆盖失败重试、重复认领、错误/过期完成及过期调度更新 |
| WASM 初始化执行额度为零 | 新增实际 WASM 模块测试中，带数据初始化/start 函数的合法模块加载失败，报 all fuel consumed | 实例化前设置有限额度；覆盖初始化、无限循环受限、调用耗尽后额度重置、结果越界，并保留 trap 错误链 |
| 插件签名接受弱公钥伪造 | 身份元公钥与特制 R/S 在原 `verify` 下能验证任意插件内容 | 使用 Ed25519 `verify_strict`；验证正常签名、内容篡改、错误/未知公钥、弱公钥伪造；签名和 WASM 功能分别及组合加入 CI |
| stdio 插件跳过已配置的摘要校验 | 实际进程测试证明经 PATH/cwd 找到的程序即使摘要错误仍可加载，替换文件后也能被自动重启 | 使用子进程 PATH/cwd 解析并固定绝对执行路径，每次启动/重启前校验；摘要失败记录为 Failed；哈希移至阻塞工作线程并使用有界读取缓冲 |
| 调度器停止通知丢失、无法重新启动 | 首次轮询前和 sink 执行中的通知都可能丢失，结束的 handle 又阻止下一次 start；三个行为回归原先失败 | 每次启动使用独立 CancellationToken，停止后完成已占用工作，允许结束后的重新启动 |
| 阻塞的调度任务阻止资源释放 | worker 在 await sink 时持有 Scheduler 的 Arc，使外部丢弃 Scheduler 后仍不能触发 Drop | worker 只持有 store/sink 依赖；Drop 能取消工作任务，回归观察 in-flight future 的析构 |
| 调度完成时间和占用凭据不准确 | 完成/退避时间在 sink 执行前采样；`job_id:attempt` 在不同 cron 周期重复，允许旧请求命中新占用 | 完成后取时间，保留 claim/finish 两个历史时间，退避从完成算起；独立 UUID 占用凭据与稳定业务 delivery key 分开，覆盖跨周期迟到完成 |
| 调度持久化失败被报告为成功 | SQLite 写入失败退化为日志；读取失败或损坏 JSON 退化为空列表/未找到 | 存储和调度 API 返回 Result，贯通 cron 工具、插件 RPC、活动与状态查询；真实 SQL 故障验证创建失败不会报告成功，HTTP 状态查询返回共享 problem envelope |
| 并发提示词/暂停修改被覆盖或没有落库 | 新增回归复现提示词改变仍可被旧轮询覆盖，执行中的暂停返回成功但未保存 | 比较完整持久化 JSON 与占用者，冲突显式返回；完成前重读最新配置并比较保存，暂停/恢复/更新保留正在执行的 occurrence；重复 ID 的插入被拒绝 |
| 进程退出后的调度占用永久卡住 | 已认领行永远排除在 due 查询之外，未实现注释承诺的恢复 | 每 30 秒续租，90 秒无续租后可接管；逐项认领避免排队消耗租期；真实子进程无析构退出后，新进程保留业务幂等键恢复，旧占用不能续租或完成 |
| 调度状态和中央历史不原子 | 完成与历史分开写；清理失败仍提交历史插入，回归观察到 1,001 行突破 1,000 上限 | 状态变化、新历史、历史清理同事务提交；任一语句失败整体回滚；内存存储用同一把锁，并按完成时间保留历史 |
| 续租可能阻止完成事务推进 | 真实 SQLite 写锁回归复现：完成事务持有连接，续租等待同一连接，完成 future 又停止被轮询 | 同时轮询完成与续租 future，已完成的提交优先；释放外部写锁后可立即完成和记录历史 |
| 有限 cron 的最后一次执行被当成错误 | 执行完后没有未来时间被视为异常，旧 worker 删除任务；保留占用后又会反复恢复 | 最后一次正常完成或 misfire 变成 completed 状态并保留历史；其他完成错误保留持久化行和诊断 |
| 抓取浏览器关闭偶发失败 | 全工作区及独立重跑均复现：先杀主进程，再调用 kill 工具，进程组消失时返回失败 | Unix 先发进程组信号，直接识别 ESRCH；已回收 PID 不再用于组信号；新增自建进程组用例，现有 lifecycle 测试保留真实错误 |
| stdio 正常退出被误重启，退出状态不准确 | 实际子进程回归中，OnFailure 和 Always 同样重启；信号退出还会保留上次数字退出码 | 独立 supervisor 读取 OS 退出状态；OnFailure 区分正常退出与进程/协议/管道失败；正常停止为 Stopped，信号退出清除旧 code |
| stdout/stdin 被当成完整进程生命周期 | 后代持有 stdout 会隐藏主进程退出；stdin 写入失败后 transport 仍显示 Running；EOF 早于退出也被误判 | 同时监控进程、读取和写入；1 秒窗口协调 EOF/退出并保留最后完整响应；清理进程树，无法确认清理时不启动替代进程 |
| 旧 stdio 进程的回调和请求缺少代次隔离 | 实际回归观察到旧进程的宿主回调/通知 future 在重启后仍存活；请求登记与进程切换也没有原子边界 | 每代使用独立取消令牌并等待任务结束；请求登记绑定当前进程，回调写入和流注册拒绝替代进程；关闭结果可重复观察 |
| stdio 日志无界分配且遇非法 UTF-8 停止读取 | 100 KB 无换行输出持续扩展单行；非法字节结束 stderr drain，后续大量日志导致真实 RPC 断开 | 每段最多保留 16 KiB 原始字节，替换非法 UTF-8 并继续读取，标记延续段；覆盖跨段 emoji 和全部输出保留 |
| stdio 流注册导致事件乱序 | 多线程真实进程用例复现 first/second 被交付为 second/first | 流注册、早到事件回放和后续交付共享同一状态锁；重复活动 stream ID 被拒绝，旧流失败不干扰新进程复用 ID |
| stdio 流结束标记静默删除数据 | 注册前的 64 个 chunk 后接 end，回归收到 0 个 chunk，却仍返回成功；溢出错误也可能被后续事件抹去 | 64 个早到 chunk 与 terminal 分开保存；溢出错误保持至交付，后续成功不能覆盖；覆盖 64/65/128 个早到 chunk |
| 完整宿主把未初始化的替代进程报告为 Running | 真实 PluginHost 回归证明自动重启跳过 `meta/init`；初次激活末尾还把实际 PID 覆盖为 None | 每次重启重新校验不可变 manifest、执行有界 init、签发新的短期回调授权；验证 protocol version，完成前拒绝普通请求和通知，保留真实进程状态 |
| 复用 stdio 进程仍回调旧宿主、更新旧状态表 | 完整宿主回归中，新宿主收到旧 HostClient 的结果，也看不到重启状态更新 | 在同一次启动锁事务中转交回调路由、状态表、资源所有权及未来初始化参数；复用不对当前进程重复 init |
| 动态注册与进程生命周期错位 | 死进程工具在重启后残留，活进程工具在宿主复用后丢失；manifest 默认值覆盖动态定义 | 回调使用进程子作用域；转交全局工具、会话覆盖、显示和主题，进程退出时清理；manifest 默认值在初始化或转交之前安装 |
| 取消初始化或交接留下仍接受调用的进程 | 三个新增用例分别复现交接中通知仍成功、取消交接后进程未退出、取消初始化后未清理 | 交接暂停普通请求准入；未完成的交接终止进程并记录失败，初始化取消也终止对应进程代次；回调/状态/参数最终发布之间不再存在 await |
| 宿主构建取消遗留已经转交的进程 | 第二个插件 init 阻塞时取消整个构建，第一个已复用进程继续运行但已失去可用新宿主 | 构建守卫关闭已经转交的 transport 并在旧宿主记录失败；旧宿主 shutdown 仍清理自身注册项，成功转交的进程继续由新宿主运行 |
| effect 清理依赖首个等待者持续存活 | 取消首个 dispose 等待者后，其他调用永久等待；同步/异步清理器 panic 也中止后续清理 | 独立 Tokio 任务执行一次清理并共享结果；逐个捕获清理器 panic、记录失败并继续释放其他资源 |
| 关闭包装层阻止强制清理并抹去失败 | 强制 close 先等未完成请求，永远无法到达底层关闭；第二次 close 把失败变成成功；graceful 超时未升级清理 | 强制 close 先关闭底层再等待包装层退出，保留同一结果；10 秒 graceful 期限失败后再进行最多 5 秒的强制关闭，并保留主要错误及清理诊断 |
| 显式重载继续复用不可用的 stdio transport | 正常停止、重启预算耗尽和包装层已关闭的三个完整宿主回归原先失败；旧宿主还可能取回已经转交给后继的进程 | 规划及实际交接检查进程状态和精确作用域所有者；失去资格时重新创建，真实 provider 实例替换会重启直接及传递硬依赖，独立插件保留复用 |
| 清理期间拒绝注册却已经修改可见状态 | 工具和主题返回错误但替换已发布值，display 还返回成功；先释放旧清理器也会丢失清理归属 | 接纳清理器成功后，在注册表锁内更新全局值和归属；拒绝时保留原值和清理器，已关闭作用域不会隐式复活 |
| 交接中关闭仍报告绑定成功 | 持有旧进程 lease 暂停交接，开始 close 后再释放 lease，回归观察到交接仍成功 | 最终发布重新核查进程和 transport 状态，已关闭或取消的进程不能完成绑定；交接开始后的失败继续清理部分转交的进程 |
| SQLite 失败记录和子任务生命周期约束可被绕过 | 实际 SQL 接受 `{}`、`null`、数组及缺字段对象；正式存储 API 还能把子任务状态写成 NULL；插入未验证子任务终态时间和失败记录 | JSON 类型判断使用可识别 SQL NULL 的比较；插入和更新均验证子任务生命周期，拒绝写入时原记录与版本号不变；既有版本 13 数据库也在事务内更新触发器 |
| SQLite 仅凭版本号接受损坏或不兼容结构 | 只设版本 13 的空库、增加旧列或删除必需索引的库均被接受；非空零版本数据库被补入 Agena 表；拒绝旧版本前已经改变 synchronous | 创建前要求零版本库为空，当前版本核对实际表和必需索引定义，拒绝发生在 PRAGMA 修改前；保持版本 13 和现有表结构，不迁移不兼容数据库 |
| UTF-8 文件分块返回无法推进的游标 | `limit=1` 读取 emoji 返回空内容和相同 nextOffset；文件末尾缺少 UTF-8 后续字节也一直报告 hasMore | 正数 limit 的有效下限为 4 字节，EOF 的不完整编码明确报错；保留前端依赖的 `limit=0` 元数据请求，检测打开时的长度变化和短读 |
| 不覆盖上传存在检查与写入竞态 | 24 个并发 HTTP handler 请求中，20 个都报告创建同一文件成功 | 在同目录完整写入临时文件，以内核不覆盖发布操作决定唯一成功者；已有普通文件和符号链接均不会被不覆盖上传替换 |
| 普通文件写入失败会破坏原文件或留下半成品 | 独立子进程将 RLIMIT_FSIZE 设为 4 KiB，内核部分写入后返回错误；上传和文本写入的四种已有/新建目标都受到破坏 | 上传与文本写入共用有界阻塞 worker，完成临时文件写入、同步后再原子发布；保留文件 mode 和已有符号链接，执行原目标写权限检查；回归验证失败时原文件保留且临时文件清理 |
| 内容替换损坏原文件、突破输出上限且遗漏部分完成 | 路由回归复现普通展开和单个捕获展开写入 64 MiB；内核部分写入破坏原文件；批量失败不报告已完成项 | 单文件原子发布，生成期间限制 50 MiB，保留失败状态并返回已完成明细；搜索 revision 和进程内锁减少过期/并发覆盖，外部写入竞态仍有明确边界 |
| 混合替换范围突破候选上限且误包含隐藏文件 | 两个回归分别得到 4,001 个候选、在 includeHidden=false 时仍改写隐藏文件 | 目录展开和明确文件共用 4,000 个候选预算，目录遵守隐藏/Git 忽略设置；明确文件选择保持有效 |
| 整文件读取在预检后突破上限、错误分类不准 | 三个正式路由均在预检后返回 52,428,801 字节；文件被换成目录和无效 UTF-8 被当成 500 | read/raw/download 共用有界 reader，检查打开后的文件类型和大小，实际读取最多上限加一字节；400/404/413 明确分类，二进制内容与响应头保持原契约 |
| SQLite 通用事务清理错误覆盖主要错误 | 真实 RAISE(ROLLBACK) 触发器的错误码 1811 被外层回滚错误 1 覆盖，应用自己的错误变体也丢失 | 两个通用辅助函数保留原错误，另行记录回滚诊断；验证日志、未执行 effects、原行回滚和连接恢复 |
| SQLite busy 结构化分类与退避起点错误 | 扩展码只匹配错误的 31；消息判断会覆盖非 busy 代码；真实 WAL 快照冲突返回 517，原消息回退使该常见实例仍被识别 | 以低字节主码 5 识别扩展码，仅无数值码时回退消息；退避从 100 ms 起，真实锁争用验证操作和 effects 仅执行一次 |
| parts 的 UPDATE 绕过内容与生命周期约束 | 直接 SQL 接受非法 JSON、错误类型的 run_kind/abort_reason、移除终态原因及负数开始时间；两个后端的正式 API 也接受无效 run 内容 | INSERT/UPDATE 均核对 JSON 与控制字段，补时间戳更新约束；已存在版本 13 在重开时刷新触发器，保留表、索引和数据 |
| 内存 part 更新失败后状态已经改变，完成返回值与落库值不符 | 状态更新后追加非文本内容返回错误，但内存 part 留在 in_progress，版本未推进；代码审查发现 SQLite complete_run 返回旧 revision/updated_at | 两个后端共用候选更新/完成逻辑，校验成功后发布；正式 API 回归核对错误后的 part/session 版本和完成后的返回值与落库值一致 |
| 旧所有者清理误删后继的全局注册 | 工具、display、theme 的相同内容替代值均被旧进程清理删除；旧 manifest 清理也删除后继实例的工具 | 每次注册保存独立身份，清理时在注册表锁内核对；替换退休原所有者的精确 handle，manifest 默认工具逐项归属 |
| 进程贡献交接混入根或兄弟作用域的值 | 三项回归中，工具快照带走已被根替换的值，display/theme 快照还包含同插件其他作用域的独立条目 | 三类全局快照均按注册表中当前的精确作用域归属选择，导入后由接收进程负责清理 |
| 全局显式删除留下原所有者的活动清理器 | 根作用域删除子进程的工具、display、theme 后，子进程 inspect 中仍有活动 effect | 从已删除条目的归属释放实际所有者的 handle，保留同插件内全局删除语义和不同插件的名称隔离 |
| 工具删除监听器在注册表写锁内运行 | 新回归用 try_read 证明监听器无法读取已提交的删除；正常阻塞读会等待自己尚未释放的写锁 | 捕获 mutation generation 后释放锁，再发送删除事件；监听器可读取删除后的表与一致的事件版本 |
| ScopedRegistry 在所有者准入前公布条目 | 两个入口均可观察到最终被拒绝的 candidate；并发清理后仍返回成功注册，或删除条目后留下活动 effect | 两种登记入口共用原子发布实现，每个可见条目必须携带完整清理 handle；准入失败保留值、作用域元数据和注册代次 |
| hook/transport 登记缺少精确所有权 | 旧作用域清理删除后继条目；停止准入后登记仍改变可见目录和路由 | 共用贡献注册表，按每次登记的身份清理，拒绝时保留原值；构建传递登记失败后回滚对应作用域 |
| 宿主回退清理跨 await 删除后继资源或留下半清理元数据 | 分别阻塞 token 和 service-binding 锁后发布后继，旧清理仍删除新值；取消等待时 token 已先被删除 | 先获取全部异步锁，再在精确代次读锁内同步完成回退清理；等待期间取消保留回退元数据 |
| 同一插件并发首次使用创建多个逻辑作用域 | 16 个同时开始的线程在首轮得到代次 7 和 8 | 单一写锁内使用 entry 创建作用域，所有调用共享同一实例，保留关闭后不隐式重建的规则 |
| ScopedHostClient 在停止准入后仍执行回调 | quiesce 后生命周期仍为 Active，配置读取、目录读取、通知和日志继续成功 | 60 个入口共用精确作用域准入和 lease；停止或过期返回 HostUnavailable，无返回值的日志直接丢弃 |
| 作用域清理不等待进程内的在途回调 | 已阻塞回调的 active_leases 为 0；dispose 后回调仍 Pending，等待 host-client 锁的调用也未取消 | lease 覆盖锁等待和宿主调用，作用域取消先释放回调 future 再释放 lease；主动请求自身清理也可结束 |
| 在途进程内回调借用后继的登记所有权 | 在宿主操作中切换实例后登记主题，条目归属后继，原作用域无对应 effect | task-local 保留准入时的 owner，并绑定宿主的精确弱引用，避免不同宿主的同名插件混用所有权 |
| 本地插件目录操作绕过回调授权 | 未签发的 session_id 可登记/替换/删除/读取会话工具；过期 token 可删除主题，修改 token 对应会话仍能写入另一会话 | ScopedHostClient 共用入口先校验上下文，再执行任何本地或转发操作；拒绝保留目录、事件版本及 effects |
| 已验证或新签发上下文混入外层字段 | 缺省上下文的 RPC 读取外层会话工具；新 token 混入未签发的 workspace/tool 后无法通过自身校验 | 增加完整设置上下文的 SDK 入口，宿主认证边界和两条流调用路径使用它；内部补丁式继承保留原语义 |
| 格式错误的 RPC 上下文被降为全局调用 | 目录读取忽略解析错误，空数组还可被 Serde 解析为空结构体 | 非 null context 必须是 JSON 对象并能正确解析，否则返回 InvalidParams；缺省/null/空对象保持插件级语义 |
| SDK HTTP 流回调丢失调用授权 | 实际 HTTP 数据包中的 chunk 和三种 terminal 路径缺少 plugin_id/authority_token；普通配置回调仍带有授权 | 从收到的 Request.context 保存身份和授权，插件任务与所有流转发使用同一上下文；并发流隔离有真实 HTTP 回归 |
| 独立 HTTP 请求借用中间件的权限上下文 | 普通与流调用未带 Request.context 时，回调继承中间件的 test-authority-999 | 两条 HTTP 分发路径使用完整设置上下文的入口；缺省请求不继承外层身份或授权 |
| HTTP 流入口跳过短期回调授权校验 | 持有插件 bearer 的真实 HTTP 请求用空对象/空数组、伪造/修改/过期 authority 发送流事件，bridge 仍返回成功 | 流入口在 transport 分发前校验 host authority，HTTP bridge 要求 context 为对象；拒绝未签发、过期和其他插件的授权 |
| HTTP 流仅按远端 ID 路由，允许不同调用串流 | 一个仍有效的调用授权可向另一条流注入 chunk、end 和 error | 待注册调用及活动流保存完整调用上下文，事件必须与目标流一致；早到 ID 与最终响应绑定，重复 ID 不替换已有消费者 |
| HTTP 早到结束包丢弃数据或覆盖溢出错误 | 64 个早到 chunk 后接 end，消费者收到空数据；128 个早到 chunk 后接 end，溢出被报告为成功 | 同一状态锁处理注册、回放和交付；64 块与独立 terminal 槽分开，首个终态和溢出错误保留至交付 |
| HTTP 插件复用后仍使用旧宿主凭据 | 实际 SDK 插件从宿主 A 复用到 B 后，配置回调报 invalid or missing plugin callback bearer token | 同一 HostClient 代理通过 HTTP 控制方法比较并替换地址和 bearer；无需重复 init，旧宿主关闭后仍可使用 |
| 候选 HTTP 插件在发布前回调失败 | bridge 仍选择 A 时，新远端插件在 B 的 init 中读取配置失败，B 未加载工具 | 一个重载谱系共享弱凭据路由，回调按签发凭据的 HostHandle 和精确作用域准入，支持显式配置的候选宿主 |
| HTTP 凭据和异步派发缺少同一作用域准入 | 旧入口先验证 token，再分开读取插件和派发；等待锁、取消和 scope 替换存在检查间隙 | 在 token 锁下重查精确 scope，持有 lease 覆盖后续等待和操作；取消丢弃 future，注册保留准入时的 owner |
| 热切换错误被无人读取的流阻塞 | 真实 HTTP 流累计交付 48 个 chunk 后切换宿主，terminal receiver 超时 | 生命周期转发同时等待终态，错误可中断 chunk 发送等待；成功流仍完整交付排队数据 |
| 旧宿主 shutdown 关闭重新初始化后的 HTTP 插件 | settings 改变后，新 transport 初始化同一个远端；旧宿主随后关闭导致新工具报 HTTP plugin instance has shut down | 远端比较观察到的 revision，按 transport owner 隔离普通调用、shutdown 与 rebind；真实 HTTP 延迟旧 shutdown 回归 |
| HTTP 生命周期取消后清理责任丢失 | 宿主超时不保证 Axum handler 同时被丢弃；SDK 控制路径原本没有独立期限 | SDK 生命周期及后台清理各有 30 秒期限；取消/失败守卫保留清理责任，后任完成清理才可初始化；虚拟时钟和取消回归 |
| HTTP 响应未匹配请求 ID | 宿主接受其他请求的 state 响应并继续替换；SDK 将不匹配的配置回调响应报告为成功，两条实际 HTTP 用例均复现失败 | 两个方向在使用 result/error 前校验完整 RequestId；错误 state 响应不会中断旧插件 |
| HTTP 宏插件重载和失败重试重复写 OnceLock | 实际 HTTP 宏插件第二次 init 均报 plugin config already initialized，包含首次 init 已失败的情况 | router 使用工厂创建新的 Rust 对象，每实例独立 dispatcher/回调；真实 HTTP 验证对象 ID、配置变化及旧对象释放 |
| HTTP shutdown 成功后仍持有插件对象 | router 继续提供服务时，shutdown 成功但插件 Drop 尚未运行 | 完成清理后释放 dispatcher/对象，只保留实例 owner/revision；重复 shutdown 不重复清理 |
| SDK 接受不兼容的初始化结果后开放调用 | 返回不同协议版本的 InitOutcome 仍被报告成功 | 在 ready/准入前核对协议与 manifest；不兼容结果进入该实例的清理路径，重试构造新对象 |
| 生产 HTTP 插件首次初始化没有可用回调 | 真实 AgenaRuntime 中要求 init 读配置的插件未完成初始化；此前只测试显式 bridge | 在 init 前启动专用本机监听器和弱谱系 dispatcher，初始 HostClient 读取候选配置；实际开启 UI 密码的应用启动回归 |
| RuntimeHostClient 与目录监听器形成强引用环 | shutdown 插件并释放普通运行时句柄后，保留 HostHandle 仍能升级 RuntimeProcessState 的 Weak | 运行时克隆共享一个内部所有者，客户端弱绑定；监听器经弱 LiveSignalHub 同步发送；验证克隆存活及最终释放 |
| 候选 HostClient 读取已发布的旧配置 | 原适配器始终读取 current_snapshot，候选首次服务调用不能得到尚未发布的配置 | 每个候选持有完整配置 envelope，运行时服务按 generation 等待发布；真实 restart/rebind 回归 |
| snapshot 后续构建失败缺少及时插件清理保护 | cleanup guard 原本在 provider/config/LSP 等步骤之后建立，成功 init 没有覆盖整个构建期 | host 完成即安装 guard；在真实 provider.list 失败和取消时验证 shutdown 与监听端口释放 |
| 未完成候选提前替换进程可见宿主 | 原 compose_and_install 在 snapshot 服务构建前更新 host slot | 完成快照发布后安装 slot；暂停候选 provider.list 时验证 slot 与 current_snapshot 仍指向前任 |
| ScopedRegistry 的失败恢复覆盖并发后继 | 明确暂停替换、关闭旧所有者并清理层后，新所有者已登记的 generation 3 被错误恢复成旧 generation 1 | 在任何可见状态修改前完成校验和准入，移除事后回滚分支；保留严格的同所有者替换规则 |
| ScopedRegistry 饱和代次允许重复身份 | 把 next_generation 设为 u64::MAX 后登记仍成功，饱和加法无法为后续登记提供不同代次 | checked_add 在发布前返回 GenerationExhausted；验证边界失败保留值和 effect，旧手工句柄不能误删后续成功登记 |
| 无效作用域父子关系残留元数据 | 256 个被拒绝的自循环请求留下 256 个 known_scopes 条目 | 先验证父子关系，再登记作用域；重复无效请求保持父映射和作用域记录为空 |
| 插件同步自重载取消自己的调用 | 真实 HTTP 工具在作用域交接时 transport disconnected，未能返回重载结果 | 新增请求/状态接口，独立任务等待发起调用及嵌套/流终态；运行时在修改前拒绝旧同步接口 |
| bundled settings 写入后重载继承过期调用上下文 | 配置已经写入，但 provider.list 报 authority token unknown or expired | 返回 reload_task 接收结果，重载构建脱离旧调用上下文；真实失败写入 Failed 任务状态 |
| 后台任务与维护在关闭后仍被接收 | 确定暂停 Runtime 初次检查与登记之间，以及 shutdown 后直接 spawn 的回归原先失败 | 关闭准入与登记共用锁；维护 guard 登记也检查关闭；16 线程交错和实际 Runtime 回归 |
| 预取消、panic 或 observer 错误使任务状态与执行脱节 | 已取消仍调用工厂；panic 留下 Running/去重占用；observer 在锁内或使启动中断 | 取消优先并延迟构造、panic 隔离、弱 finalizer；终态与索引清理原子，observer 在锁外 |
| 终态历史上限没有限制活动任务排队 | 不同调用可不断添加等待中的重载；历史裁剪保留所有 Running | 新增共享 64 个活动任务上限，先去重后拒绝容量；释放名额后真实 HTTP 插件可再次重载 |
| SDK 回调和目录服务丢失失败类型 | 实际 HTTP 插件将容量错误读成 Internal；目录刷新 REST 满载返回 500 | HTTP/stdio/Host 共用 typed RPC 解码；目录准入保留结构化错误，Application 统一 503/404/409 与重试语义 |
| 已发布重载被附带维护入队失败标成 Failed | 真实重载已交换快照，可选目录刷新因容量无法登记，整个任务仍失败 | 已完成重载保留 Succeeded；目录失败记录在 last_failure，测试同时核对代次和失败状态 |
| 候选提供方版本泄漏到现役请求 | 未发布、失败和取消的候选都使实际 HTTP 模型发现使用候选 User-Agent，现役仍为 generation 1 | 每个快照、适配器及认证句柄持有不可变 ProviderClientIdentity；回归覆盖候选、保留旧注册表和两个并存 Runtime |
| 插件收到提供方 CLI 版本作为宿主版本 | 实际 HTTP init 的 agena_version 为配置中的 Codex 0.111.1，实际 Agena 是 0.1.2-beta.1 | composition 传入 Agena 包构建版本；初次及修改设置后的重新初始化均验证 |
| 顶层关闭重复或提前发送会话结束通知 | 实际 HTTP 复现重复/16 线程并发关闭的重复通知，持有发布锁时通知也已到达；无 executor 的第一次诊断仍见运行中状态 | 在发布互斥锁内决定首次关闭归属；关闭准入后由唯一调用者安排广播，无 executor 只在关闭后诊断一次 |
| 会话结束广播在排队或批次中途选中后继宿主 | 正式 reconfigure 后，原宿主漏收同批的第二个会话；尚未轮询的广播也改用后继 | future 构造时捕获宿主，异步枚举出的整批会话均使用该宿主；实际会话/HTTP 屏障回归 |
| 实际应用回调测试提前认定重载完成 | 独立重复第 10 次读到候选配置后，下一次重载返回 HostUnavailable；候选配置可见不代表发布完成 | 保留 REST 接受任务 ID，查询其成功终态后再发起 SDK 重载；完整重跑该组 20 次 |

## 首批基线验证

- Web：Bun **1.3.14**，冻结锁文件安装成功；**470 项测试通过**；生产构建、
  类型检查、导入规则和设置翻译检查通过；依赖审计 **0 条漏洞记录**。
- Rust：`cargo test --workspace --locked --no-fail-fast` 在 macOS 返回 0。
  日志汇总 **2,341 passed / 0 failed / 5 ignored**，另有 27 项宏编译 fixture 检查通过。
  包含 4 项 MCP HTTP 集成测试、3 项服务重启/进程所有权测试及插件 panic 隔离回归。
- 格式检查通过；最终格式/全工作区测试/Clippy 执行前后的 tracked source diff 完全一致。
- 运行时能力：macOS 实际执行的 AWS HTTPS 和 reqwest HTTPS 构造测试均通过，未再跳过执行。
- Clippy：`cargo clippy --workspace --all-targets --locked -- -D warnings` 通过。
- Python：使用 Python 3.13，5 项 refactor 工具测试通过；19 项失败语义不变量通过。
- 脚本：依赖脚本通过 ShellCheck，并通过两个模式的隔离命令桩执行，确认实际进入当前 Web 目录。
- CI：新增 workflow 通过 actionlint；尚未推送或执行远端 GitHub Actions。
- 浏览器：已尝试 Browser 连接及官方排查流程，当前可用实例列表为空；未完成可见页面检查。

本地验证日志和机器可读汇总保存于
`.tmp-artifacts/technical-debt-audit-2026-09-06/`（被 Git 忽略的本地产物）。
该轮覆盖默认 Rust 功能集合；被忽略测试、额外功能和其他平台均不算已验证。

## 后续验证（2026-09-06 01:06）

在上述依赖、调度存储、dist、WASM 和签名修复完成后重新执行：

- `cargo test --workspace --locked --no-fail-fast`：**2,343 passed / 0 failed / 5 ignored**，
  另有 27 项宏编译 fixture 通过；包括实际 MCP/restart 集成测试和使用 Tantivy 的 memory-index 测试。
- 插件 host 功能分别执行：`signing` **75 项通过**，`wasm` **77 项通过**，
  `--all-features` **79 项通过**。这些组合包含重复执行的共同测试，不与 workspace 数量相加。
- 工作区 Clippy（全部 targets）及插件 host Clippy（全部 targets/features）均在 `-D warnings` 下通过。
- 实际 dist 可执行程序验证同步/异步 panic 捕获以及后续 ping 成功，退出码 0；
  修复前同一用例退出码 134。
- AWS/reqwest 运行时能力检查通过；Rust advisories 检查通过，没有 yanked 警告，
  仍保留本文说明的 RSA 例外。
- 格式检查、ShellCheck 和更新后 workflow 的 actionlint 通过。actionlint 的在线模块元数据请求
  超时后，直接使用已缓存的 v1.7.11 源码离线运行成功。
- 全部 Cargo 验证执行前后的 tracked source diff 一致。新增日志及机器可读结果见同一产物目录中的
  `followup-verification.json`；`verification.json` 保留首批基线结果和当时的限制。

这些结果仍只证明本机和明确列出的功能组合，未执行远端 CI 或其他平台的运行测试。

## stdio 校验与调度生命周期验证（2026-09-06 01:46）

- 先用真实 stdio 子进程复现：错误摘要仍能加载，替换执行文件后仍能重启；修复后
  三项回归通过，包含 PATH、相对路径、插件 cwd、正确摘要正常通信和重启拒绝篡改。
- 调度器的三个新增生命周期用例修复前均失败；修复后完整 scheduler **23 项测试通过**，
  另覆盖跨 cron 周期的迟到完成请求、长期执行后的历史时间和退避时间。
- 再次运行整个工作区：**2,348 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture** 通过。
- 插件 host 的 `signing`、`wasm`、全部功能分别为 **78 / 77 / 82 项通过**。
- 工作区及插件全部功能的 Clippy（全部 targets，`-D warnings`）通过；格式、真实 dist
  panic 隔离程序、运行时 crypto/auth 检查和 Rust advisories 检查通过，RSA 例外仍保留。
- 这组验证执行前后的 tracked source diff 一致，记录见产物目录中的
  `lifecycle-verification.json`。没有提交、推送、部署或执行远端 CI。

## 调度持久化、恢复与调用链验证（2026-09-06 02:30）

- 最终 `cargo test --workspace --locked --no-fail-fast`：**2,365 passed / 0 failed / 5 ignored**，
  另有 **27 项宏编译 fixture** 通过。仍包含实际 MCP HTTP/restart 集成测试。
- 调度器单独执行：**37 项通过**（35 项单元测试、2 项进程恢复集成测试入口）。独立编译
  同时发现并补齐恢复 fixture 的 Tokio `process` 测试依赖，避免依赖 workspace 的功能合并。
- 新增真实 cron 工具调用回归和 HTTP `/api/v1/runtime` 回归，验证存储失败沿调用链返回。
  子进程恢复使用临时文件数据库，进程在已持久化占用后直接退出，不运行清理析构。
- 写锁用例在修复续租轮询前超时失败，修复后通过；其他新增证据包括同一任务的并发修改、
  暂停/恢复期间完成、丢失占用时取消 sink、过期凭据拒绝、历史插入与清理失败的事务回滚。
- 工作区 Clippy（所有 targets、`-D warnings`）、格式、19 项失败语义不变量和 diff 检查通过。
  全部受版本控制和新增源码在最终验证前后的哈希一致；随后仅更新文档和 CI 的独立调度测试步骤。
- 浏览器 lifecycle 测试修复后连续 30 次独立进程运行通过，另外有不依赖 Chrome 的自建进程组
  关闭用例。现有 lifecycle 测试在没有可用浏览器时会直接返回，因此这些结果不等于可见页面验证。
- 在线 Rust advisory 库刷新超时；`cargo deny --offline check advisories` 使用缓存通过。
  缓存提交为 `5a0ebedfe8bdd2e295b171f4162f8c977bcad9a5`，提交时间
  `2026-09-02T11:13:32+02:00`。仍保留 RSA 例外，未声称已取得最新在线漏洞数据。
- 最终记录为同一产物目录的 `persistence-verification.json`；初次运行、注入故障和修复前
  失败日志独立保留。没有提交、推送、部署或修改已安装 Agena 服务。

恢复语义仍是 **at-least-once**：90 秒租期届满后由下一轮轮询恢复。进程长时间挂起、显著
时钟调整、在外部系统已接受动作后退出，都可能造成重投，必须由 sink 使用稳定业务键去重。
这次沿用 schema v1，没有迁移或清空任何数据库。运行时现有 session sink 有持久化去重；
任意第三方 sink 的外部效果不能因此被描述成 exactly-once。

## stdio 监督、流输出与日志验证（2026-09-06 03:14）

- 最终工作区：**2,380 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture** 通过。
  本轮仅修改 Rust 插件 host 及其测试，没有重跑此前已经通过的 Web 验证。
- 插件 host 单独执行：默认功能 **88**、`signing` **93**、`wasm` **92**、全部功能 **97** 项通过。
  功能组合重复执行共同测试，不与工作区数量相加；摘要校验、签名、WASM 和 panic 回归仍通过。
- 新增 **15 项** Unix stdio 集成测试，使用明确配置的临时 Python 子进程。其中多线程流测试
  在修复前观察到确切乱序；缓冲测试在修复前观察到 64 个 chunk 全部丢失。整组测试独立
  连续执行 **20 轮**全部通过，包括每轮重复 10 次的退出前最终响应交付。
- 首次生命周期回归为 **5 失败 / 1 通过**。后续回归另捕获 stderr 非法 UTF-8 断流，以及
  macOS 进程退出时组信号短暂返回 EPERM。后者先等待回收再重试，只有重试确认清理成功
  才允许重启，未将权限错误直接忽略。
- 工作区和插件全部功能的 Clippy（全部 targets、`-D warnings`）、格式、19 项失败语义
  不变量及 diff 检查通过。实际 `dist` 可执行程序捕获同步/异步 panic，后续调用正常，退出码 0。
- 最终验证前后全部受版本控制及新增源码哈希一致。记录见同一产物目录的
  `stdio-verification.json`；随后仅更新本文和 `docs/development.md`。
- 当前证据来自 macOS ARM64 和真实本地子进程。没有执行远端 CI、Windows/Linux 运行测试
  或可见页面验证；未提交、推送、部署，也未停止或修改已安装 Agena 服务。

该检查点验证的是 transport 的进程与消息生命周期。应用初始化和宿主交接的后续验证见下一节。

## 完整插件宿主、交接与清理验证（2026-09-06 04:18）

- 最终工作区：**2,404 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture** 通过。
  `PluginEffectScope::dispose` 的 Arc 接收者改动及 transport 初始化接口已通过整个工作区编译和测试。
- 插件 host 单独执行：默认功能 **112**、`signing` **117**、`wasm` **116**、全部功能 **121** 项通过。
  各组合重复执行共同测试，不能把这些数量与工作区相加。
- 新增 **16 项**完整 PluginHost 的 Unix 实际进程回归；与此前 **15 项**底层生命周期回归一起，
  连续 **20 轮**独立执行全部通过。覆盖会话工具隔离、显示/主题转交、重启后的释放、
  新宿主版本和工作区参数、旧回调取消、旧作用域释放及整个构建取消后的已转交进程清理。
  无在途回调的基本 stdio 宿主释放用例还检查了弱引用释放和 OS 子进程退出。
- 最初六个完整宿主用例全部失败；随后另复现默认工具覆盖动态定义、初始化时接受通知、
  三个交接/初始化取消问题、旧作用域未释放、整个构建取消遗留进程、两个 effect 清理问题，
  以及三个强制/重复关闭问题。修复前日志保留于同一产物目录；中间 fixture 编译或参数错误
  不作为产品缺陷证据。
- 工作区及插件全部功能 Clippy（全部 targets、`-D warnings`）、格式、19 项失败语义不变量
  和 diff 检查均通过。实际 dist 可执行程序仍能捕获同步/异步 panic，后续调用成功，退出码 0。
- 验证前后全部受版本控制及新增源码哈希相同。记录为 `stdio-host-verification.json`，
  驱动为 `validate-stdio-host.py`；早先的 `stdio-verification.json` 等检查点保持独立。
  最终验证之后仅更新本文和 `docs/development.md`。
- 本轮修改 Rust 插件 host 及其测试，没有重跑 Web。结果来自 macOS ARM64；未执行远端 CI、
  Linux/Windows 运行测试或可见页面验证。已安装 Agena 服务仍在运行，没有提交、推送或部署。

交接失败的语义是**停止无法完成交接的进程并报告失败**。整次宿主重载仍不是恢复旧宿主的
原子事务：已转交的插件在后续构建失败或取消时会被关闭，不保证旧插件集继续可用。异步清理
也仍依赖 Tokio 运行时继续调度；任意自定义清理器永久阻塞的问题不能由 panic 捕获解决。

## 重载恢复与注册所有权验证（2026-09-06 04:53）

- 最终工作区：**2,418 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture** 通过。
  插件 host 默认、`signing`、`wasm`、全部功能分别为 **126 / 131 / 130 / 135 项通过**。
- **23 项**完整宿主与 **15 项**底层 stdio 回归一起连续 **20 轮**独立执行全部通过。
  新增用例覆盖停止/耗尽预算/已关闭后的重载、旧宿主不能夺回后继进程、provider 实例替换后的
  直接和传递硬依赖重启，以及等待其他插件初始化时复用候选退出。
- `can_reuse` 只给规划提示，`try_rebind_host` 在实际交接处再次确认所有权和可用性；尚未开始
  交接时失去资格会转为新建。规划后的新建仍须匹配已验证 manifest，变化会阻止激活并清理新
  作用域；下一次显式重载可以重新解析并恢复。该路径同时验证没有遗留工具或开放作用域。
- 工具、显示、主题的三个并发清理回归修复前均失败；替换登记现在先取得所有权再发布状态。
  另验证被拒绝的替换仍运行原有同步/异步清理器、旧作用域不会被隐式重建，以及工具事件监听器
  可以读取刚发布的注册表。交接中的 close 也有独立失败前/修复后证据。
- 工作区及插件全部功能 Clippy（全部 targets，`-D warnings`）、格式、实际 dist panic 隔离、
  19 项失败语义不变量和 diff 检查均通过。最终验证前后源码哈希一致；记录为
  `reload-ownership-verification.json`，早先检查点保留。随后仅更新两份开发/审查文档。
- 本轮仍只验证 macOS ARM64 上列出的 Rust 路径；没有重跑 Web、执行远端 CI 或可见页面检查。
  完整重载回滚、非 stdio 的动态贡献和凭据转交仍保持开放。

## SQLite 失败约束与 schema 生命周期验证（2026-09-06 05:16）

- 最终工作区：**2,427 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture** 通过。
  SQLite 包独立运行 **59 项执行通过**：58 个顶层用例，以及双进程用例在子进程中再执行的 1 项。
- 新增 8 个回归在修复前失败，日志为 `sqlite-invariants-before.log`。证据包括 SQL NULL 三值逻辑
  绕过最小失败字段、子任务插入缺少生命周期约束、正式 API 接受空状态/不完整失败对象、旧触发器
  重新打开后仍有效，以及版本号与实际结构不一致的接受行为。之后增加独立连接的并发刷新用例。
- 6 个并发入口（含真实双进程冷启动）连续 **20 轮**执行全部通过；新的文件数据库用例验证两个
  连接同时刷新旧触发器后都拒绝无效失败记录，原会话标题、状态和版本号保留。
- 当前表结构版本保持 **13**。SQL 声明比较确认：仅修改 2 个触发器、新增 1 个触发器，没有改变
  表或索引定义；本地 SQLite 3.51.0 的 `VACUUM` 也保留这些标准声明。当前版本只刷新缺失或变化
  的已知触发器；再次打开不改变 schema cookie。历史触发器 SQL 固定为测试 fixture。
- 工作区 Clippy（全部 targets，`-D warnings`）、格式、19 项失败语义不变量和 diff 检查通过；
  最终验证前后全部 tracked/untracked 源码哈希一致。记录为 `sqlite-invariants-verification.json`，
  声明差异为 `sqlite-declaration-diff.json`；随后仅更新两份开发/审查文档。
- 本轮验证使用临时或内存数据库，未访问个人数据库。没有重跑 Web 或插件额外功能矩阵；这些
  路径保留上个检查点的证据。当前平台仍为 macOS ARM64，没有执行远端 CI 或可见页面检查。

数据库触发器在此验证的是失败记录的最小字段类型，不替代完整 `Failure` 契约反序列化。
刷新触发器不会回填、重写或扫描既有业务记录；旧库已存在的损坏行识别和修复诊断仍需审查。
表和必需索引必须匹配 Agena 生成的标准声明，手工改写定义的库不会被静默修复。

## 文件读写完整性验证（2026-09-06 07:59）

- 最终工作区：**2,438 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture** 通过。
  Agena 应用包独立运行 **61 项通过**：52 项单元/handler 测试、2 项错误诊断、4 项 MCP HTTP、
  3 项服务重启/进程所有权测试。
- 新增 **11 项**文件回归连续 **20 轮**独立测试进程执行全部通过。重复驱动从 Cargo 的 JSON
  构建产物中取得实际测试可执行文件，并核对每轮确实执行 11 项，避免空过滤器造成假通过。
- 初始 6 个回归中 3 个失败，日志为 `fs-integrity-before.log`；另一个独立子进程故障用例在
  `fs-write-failure-before.log` 中复现四种写入对目标的破坏。故障通过内核文件大小限制制造，
  未耗尽开发机磁盘；限制和 SIGXFSZ 设置只作用于测试子进程。中间测试的 must-use 编译修正
  不作为产品故障证据。
- 额外覆盖并发读取只见到完整替换内容、外部反复截断时不会产生卡住的游标、默认 256 KiB
  边界的 HTTP UTF-8 拼接、零字节元数据读取、只读文件及悬空链接拒绝、有效符号链接和 mode
  保留。路径解析实际只依赖请求头/query，移除了 handler 和 resolver 未使用的 AppState 参数。
- 工作区 Clippy（全部 targets，`-D warnings`）、格式、19 项失败语义不变量和 diff 检查通过；
  最终验证前后所有受版本控制和新增源码的哈希一致。记录为 `fs-integrity-verification.json`，
  随后仅更新开发/审查文档。测试使用临时文件；未重跑 Web 或插件额外功能矩阵。
- 本轮证据来自 macOS ARM64。Unix 的文件限制/符号链接回归不能替代 Windows 验证，也未完成
  浏览器交互检查。改动尚未提交或部署，已安装服务仍运行。

原子发布保证单个路径的读者看到完整内容；它要求父目录可写，并会替换目标 inode。现有硬链接
别名、所有者、ACL/xattr 的保留及断电后的目录项持久性仍需独立评估。已经开始的阻塞写入会在
HTTP 等待者取消后继续完成；这不是撤销写入的事务。跨多个读取请求也还没有文件版本或快照协议。

## 文件请求体预算与 HTTP 边界验证（2026-09-06 08:45）

- 最终工作区：**2,451 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture** 通过。
  Agena 应用包独立运行 **74 项通过**：65 项单元/handler 测试、2 项错误诊断、4 项 MCP HTTP、
  3 项服务重启/进程所有权测试。本轮新增 **13 项**回归，其中 11 项经过正式 FS 路由构造，
  2 项验证阻塞解析边界的取消和 panic；4 项并发/取消/故障测试连续 **20 轮**独立进程通过。
- 原实际服务的 3 MiB 413 证据保留为 `fs-body-limit-v2-probe.json`。最初 9 个路由回归为
  **8 失败 / 1 通过**，日志为 `fs-body-limit-before.log`：大内容被默认提取器拒绝，body/JSON
  拒绝返回纯文本，并且没有本轮所需的大请求缓冲并发约束；其他 mutation 的默认上限原本有效。
- 上传请求体上限为 **50 MiB**；文本写入的编码体为 **300 MiB + 64 KiB**，解码后内容仍为
  **50 MiB UTF-8 字节**。多一个字节即返回 413，且不创建目标目录或损坏旧文件。上传路由归入
  FS 路由组；其他 mutation 保持默认上限，外围认证边界没有改变。
- 原始 body 按实际字节计数，不依赖 `Content-Length`；声明超限可在读取前拒绝。读取时合并
  小 frame，避免逐帧保留的额外内存。上传/写入共用两个请求名额；JSON 在阻塞线程解析，
  HTTP 等待者取消不提前释放正在运行的解析名额。读 body 时取消不会写文件；开始的文件 worker
  仍遵循上一节的完整发布语义。413、415、400、422 保留各自状态并使用 Workbench JSON error。
- 更新后的实际可执行程序运行 **17 组 HTTP 请求**全部通过，包括普通/分块的 3 MiB、50 MiB
  上传、超过内容上限、完全转义的 50 MiB JSON、编码体恰好上限和再多一字节。成功文件逐字节
  摘要相符；所有失败请求保留旧文件；之后 health 为 200。临时服务 PID **36302** 正常退出，
  并独立确认进程已不存在。可执行文件 SHA256 及各次结果见 `fs-body-limit-fixed-probe.json`。
- 工作区 Clippy（全部 targets，`-D warnings`）、格式、19 项失败语义不变量和 diff 检查通过。
  最终记录为 `fs-body-limit-verification.json`，源码哈希前后均为
  `7e232526e3bc7b0f81dd98e28302e901e580c88f23df80c833994f4e630381b3`。
  首次探针用了系统 Python 3.9 不支持的 `hashlib.file_digest`；该探针自身失败的记录另存为
  `*-initial.*`，只修改探针并重跑 HTTP，未重复已经通过的源码检查。随后仅更新开发/审查文档。
- 本轮证据仍来自 macOS ARM64、本地路由和隔离的真实服务。没有重跑 Web 或插件额外功能矩阵，
  未执行浏览器、远端 CI 或其他平台检查。已安装服务 PID **54968** 仍正常运行，未提交或部署。

两项并发限制控制请求体和解析工作，并不表示总进程内存等于文件大小；编码缓冲和解码字符串会
同时存在。当前上传仍需完整缓冲，慢请求的等待/读取超时及公平性尚待审查。文件替换仍有 inode、
外部并发编辑和多文件部分完成等上一节及下面列出的边界，不能把本轮结果扩展为全部文件操作安全。

## 普通文本替换的美元符号保留（2026-09-06 08:55）

- 已通过正式路由复现：普通模式将 `item and item` 中的 `item` 替换为 `$5`，原代码实际写入
  ` and `。原因是普通搜索内部也使用 Regex，`replace_all` 将替换文本中的美元符号解释为捕获
  引用。修复前日志 `fs-replace-literal-before.log` 为 **1 失败 / 1 通过**，正则模式作为对照。
- 普通模式现在使用 `regex::NoExpand`，回归覆盖 `$5`、`$HOME`、`${name}`、`$$` 和它们的组合；
  正则模式保留命名/数字捕获组及 `$$` 的展开行为。测试核对实际文件内容和成功替换计数。
- 最终工作区：**2,453 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture** 通过。
  Agena 应用包为 **76 项通过**，包含上述两个新路由回归。工作区全部 targets Clippy
  （`-D warnings`）、格式、19 项失败语义不变量和 diff 检查通过；请求体回归仍通过。
- 验证记录为 `fs-replace-literal-verification.json`，驱动为 `validate-fs-replace-literal.py`。
  验证前后源码哈希均为 `dad60715c6b8c00b680b61d13e50bc41e1ec9bdd769c9f52e551f2b73e278a3e`，
  随后仅更新开发/审查文档。没有重复与此分支无关的 17 组 HTTP 探针或并发重复矩阵；这些证据
  保留在前一检查点。该检查点仅处理美元符号；原子发布和输出预算见下一检查点，跨文件事务仍未实现。

## 内容替换完整性与并发验证（2026-09-06 10:09）

- 最终工作区：**2,471 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture** 通过。
  Agena 应用包 **94 项通过**：85 项单元/handler 测试、2 项错误诊断、4 项 MCP HTTP、3 项服务
  重启/进程所有权测试。本轮新增 **18 项 Rust 回归**：11 项正式 FS 路由、4 项文件 worker、
  2 项替换语法、1 项范围预算测试。7 项并发/生命周期测试连续 **20 个独立进程**全部通过，
  驱动逐轮核对实际测试数量，记录为 `content-replace-concurrency-repeats.json`。
- 修复前 **6 项回归全部失败**：`content-replace-before.log` 中 4 项复现字面量和单次捕获展开
  各写入 64 MiB、批量失败遗漏完成项、单项/批量替换在 4 KiB RLIMIT_FSIZE 下损坏原文件；
  `content-replace-scope-before.log` 中 2 项复现 4,001 候选和忽略隐藏设置。资源限制仅作用于
  测试子进程。编译、Cargo 过滤器、Bun 别名解析和 Clippy 的中间修正不计为产品故障。
- 单项/批量替换共用原子发布，生成时限制 **50 MiB**，包括一个捕获组的巨大展开；语法使用
  regex-automata 的公开插值解析器，保留字面模式和正则替换语法。无变化的单项替换不写文件，
  计数也与批量无变化行为一致。搜索和变换读取使用有界 reader，并在打开前后检查普通文件。
- 搜索返回完整原文件 SHA256 revision；可选 expectedRevision/expectedRevisions 拒绝过期
  内容，返回 **409**。非法摘要、规范化后重复路径、候选集合不匹配和未知字段在修改前被拒绝。
  未提供 revision 的客户端明确针对当前内容操作，单项仍校验 expected 文本。进程内路径锁
  协调同文件操作，命名空间锁协调参与的 upload/write/replace 与 delete/rename，发布前再次
  比较原内容。失效路径锁记录随查询清理。
- 批量中途失败保留原 HTTP 错误状态，返回 `details.completed`、`failedPath`、`remaining`。
  remaining 只数失败文件之后未尝试的文件。目录展开遵守隐藏/Git 忽略设置，混合范围共用
  **4,000 个候选**预算；明确选择单个文件不受发现过滤器影响。
- Web 全量 **475 passed / 0 failed**（172 个文件），包括 **5 项新增行为测试**。界面携带
  搜索 revision，校验部分完成明细，失败后清理缓存并刷新搜索；刷新使用现有自动刷新逻辑，
  保留请求期间形成的草稿，避免旧请求覆盖新搜索/工作区状态。生产构建、类型/导入/i18n 检查和
  改动文件 Prettier 检查通过；构建日志未出现 warning，但大资源包体积仍需后续性能验证。
- 隔离真实服务 **18 个 HTTP 请求**全部通过：revision/409、部分权限失败、恰好 50 MiB 和
  再多一字节、8 个并发独立匹配，以及 upload/write/rename/delete 冒烟。文件摘要和全部成功
  编辑均相符，请求后 health 为 200。服务 PID **44256** 退出码 0，另用 ps 确认已不存在。
  可执行文件 SHA256 为 `10aaded19bcdc57b06c3bf0911b10ae9758680c685213849e9cd6683d854ea82`，
  结果见 `content-replace-probe.json`。原有安装服务 PID **54968** 经复核仍在运行。
- 工作区全部 targets Clippy（`-D warnings`）、格式、19 项失败语义不变量和 diff 检查通过。
  最终驱动退出码 0，记录为 `content-replace-verification.json`。验证前后源码哈希均为
  `4044e37f5c282ca01073c785476b135c4249081adf9041bc486677dc9d61b7a0`，随后仅更新开发/审查文档。
  Browser 官方连接和排查仍返回空实例列表（见 `content-replace-browser-status.txt`），没有
  可见页面验证；也未执行 Linux/Windows、远端 CI 或重跑插件额外功能矩阵。

锁仅协调当前进程中参与的 Workbench 操作，不涵盖外部编辑器、其他进程或另走文件路径的
runtime/plugin 工具。外部写入仍能发生在最后比较与 rename 之间；多文件操作没有整体事务，
已启动的阻塞 worker 可能在 HTTP 取消后完成。inode/硬链接、owner/ACL/xattr、目录 fsync
边界仍如前述。该检查点尚未处理的普通 read/raw/download 读取预算见下一节。

## 整文件读取预算与错误分类（2026-09-06 10:29）

- 最终工作区：**2,477 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture** 通过。
  Agena 应用包 **100 项通过**：91 项单元/handler、2 项诊断、4 项 MCP HTTP、3 项服务重启/
  所有权测试。本轮新增 **6 项**回归，其中 5 项经过正式 FS 路由，1 项验证已打开 inode 的
  增长预算。3 项竞态回归连续 **20 个独立进程**通过，逐轮核对实际数量，见
  `file-read-races-repeats.json`。
- `file-read-before.log` 为 **3 失败 / 2 通过**。测试使用单线程阻塞池和明确的完成通知，
  在 metadata 已完成而 HTTP future 尚未继续时改变临时文件，无需生产测试钩子或概率性睡眠。
  read/raw/download 原先各返回 **52,428,801 字节**；换成目录时各返回 500；无效或 EOF
  不完整 UTF-8 也被文本读取当作 500。二进制和正常大小/响应头作为对照。
- 三个接口现在与搜索/替换共用有界 reader，普通整文件上限 **50 MiB**；不是普通文件和
  非法 UTF-8 返回 **400**，缺失文件 **404**，超限 **413**。读取期间增长也最多消费上限
  加一字节后拒绝；独立测试通过共享文件游标确认只读取 **1,025 字节**，没有读完后来增长为
  64 KiB 的文件。搜索仍跳过不适用文件，输入预算仍为 2 MiB；去除了共用 reader 外重复的
  搜索 metadata 调用。现有替换和发布测试仍通过。
- 隔离真实服务的 **27 个 HTTP 请求**全部通过：空文件、UTF-8、二进制、恰好 50 MiB、
  多一字节、目录、缺失、FIFO 和有效符号链接，分别覆盖三个接口。逐字节摘要、返回长度、
  MIME、缓存和下载头均相符，结束后 health 为 200。初始探针把 15 字节的 fixture 错写成
  14 字节而失败；改为从内容计算长度后只重跑探针，保留 `file-read-probe-initial.*`，不计
  为产品故障。两次临时服务 PID **47784 / 47853** 都正常退出且 ps 确认不存在；安装服务
  PID **54968** 保持运行。最终二进制 SHA256 为
  `595dac0a1eea0cf23884bb65e5bf52c674c67797314c9317b79ba084c21d80b2`。
- 工作区全部 targets Clippy（`-D warnings`）、Rust 格式、19 项失败语义不变量和 diff 检查
  通过。记录为 `file-read-verification.json`；首次探针失败的总记录保留为
  `file-read-verification-initial.json`。源码哈希始终为
  `71f43f2e690a9dc213b2002396f1c6a9f82fac99867deda568e6fbc9f173a075`，之后才更新文档。
  Web 沿用 10:09 的 475 项测试和构建证据；没有重复与本改动无关的额外功能矩阵、浏览器、
  远端 CI 或其他平台检查。本轮平台仍为 macOS ARM64。

读取共享 16 个阻塞 worker，HTTP 取消不会提前释放仍工作的名额。完成后的响应缓冲可能继续
等待客户端下载，因此这不是总进程内存上限；Vec 容量也不能等同于有效字节数。外部原地写入
期间仍不保证内容快照一致性。慢文件系统的截止时间、排队/已完成响应的预算和真实符号链接
工作区边界继续保持开放。

## SQLite 事务错误与 busy 分类（2026-09-06 11:17）

- 最终工作区日志汇总 **2,483 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture**
  通过。SQLite 包主进程 **64 项通过**，其崩溃锁恢复测试另启动一个子进程执行 1 项，机器
  汇总因此为 **65 passed**；这不表示 65 个互不重复用例。本轮新增 **6 项**事务回归，连续
  **20 个独立进程**全部通过，每轮核对实际执行数，见 `sqlite-transaction-repeats.json`。
- `sqlite-transaction-before.log` 为 **3 失败 / 1 通过**。真实触发器 RAISE(ROLLBACK)
  已回滚事务，产生原始约束码 **1811**；通用辅助函数再次 rollback 得到错误 **1**
  `cannot rollback - no transaction is active`，原代码将后者返回，连应用的 BusinessRule
  变体也被替换。另一项无英文消息的扩展 busy 码测试失败。真实双连接 WAL 快照冲突返回
  **517** 的对照测试原本通过：旧消息回退恰好识别常见英文诊断，不能据此声称已复现线上
  snapshot 重试失败。
- 两个导出的通用事务辅助函数现在保留原始 DbErr/应用错误，回滚错误通过 tracing 另行记录。
  回归捕获实际诊断，核对约束码和应用变体、已写行整体回滚、effects 没有执行，以及同一池
  后续仍能提交事务。延迟外键约束让真正的 COMMIT 失败，另验证两个辅助函数都不执行 effects。
- busy 分类按数值错误码的低字节主码 **5**，覆盖 **5 / 261 / 517 / 773**；明确的其他码
  不再被消息中的 `database is locked` 覆盖。只有没有可解析数值码时才回退消息。获取写锁
  的五次退避从 **100 / 200 / 400 / 800 / 1,600 ms** 开始，修正原来先递增导致首轮 200 ms
  的偏差。真实双池、busy_timeout=0 的锁争用测试验证持锁时不执行操作/effects，释放后各
  执行一次，另一连接可见已提交数据；该测试不作为严格墙钟退避时长测量。
- 工作区全部 targets Clippy（`-D warnings`）、格式、19 项失败语义不变量和 diff 检查通过，
  最终驱动退出码 0。记录为 `sqlite-transaction-verification.json`；验证前后源码哈希均为
  `729d831cb91c7890400948fe967586505d86274a82b79d14cdca12c7ea3f1ee3`，随后仅更新开发/审查文档。
  本轮只使用临时 SQLite 文件，没有改变 schema 版本、兼容性规则或个人数据库。安装服务
  PID **54968** 经复核仍运行；没有提交或部署。

本轮错误保留修复针对两个导出的通用辅助函数；当前核心 engine 和 model-catalog 的独立
事务包装仍在回滚失败时组合两份诊断，未统一它们的错误类型策略。busy 分类同时用于核心存储
和会话错误映射。提交后的 effects 仍不是持久化队列，取消/崩溃窗口仍需审查。Web、文件实际
HTTP、插件额外功能和浏览器保留各自前述证据，没有因为本轮 Rust 成功而视作重新验证。

## parts 更新完整性与后端一致性（2026-09-06 12:19）

- 最终工作区日志汇总 **2,496 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture**
  通过。`agena-storage` **55 项通过**；SQLite 包主进程 **77 项通过**，另有既存崩溃锁恢复
  子进程执行 1 项，日志汇总因此为 78。本轮新增 **13 项**回归：8 项直接 SQL/历史 schema，
  5 项正式存储 API（其中 4 项同时运行内存与 SQLite 后端）。13 项连续 **20 个独立进程**
  全部通过，驱动逐轮核对实际数量，见 `parts-integrity-repeats.json`。
- 修复前 `parts-integrity-before.log` 为 **6 失败 / 0 通过**：仅更新 content 可写入非法
  JSON、删除或改变 run_kind/abort_reason 的类型，started_at_ms 可改为负数；两个后端的
  update_part 均接受无效 run 内容。另复现内存后端先改状态，再因非文本内容拒绝流式追加，
  留下状态已变而 revision/session version 未变的记录。首次 fixture 误用 set_now_ms 方法
  的编译失败保留为 `parts-integrity-precheck.log`，不计为产品故障。
- 内容 JSON 校验补至 UPDATE，可选 provider_state 在 INSERT/UPDATE 均只接受 JSON 或
  SQL NULL；普通 part 仍可携带 JSON 标量、数组或对象。run_kind 必须是字符串，值域仍开放；
  abort_reason 存在时为字符串/null，completed 要求存在，failed/cancelled 要求字符串。
  更新 state 或 content 都检查该形状。另拒绝重复控制字段，测试实际证明 SQLite 取第一个
  同名键、serde_json 取最后一个，避免两层对同一原始 JSON 的解释不一致。
- 两个后端共用准备更新/完成的函数。内存后端只在完整校验成功后替换 part；SQLite 接管刚
  读取的候选值，避免额外复制整份内容。正常运行创建、扩展字段、完成与 provider_state 仍有效；
  正式 API 核对完成返回的 revision/updated_at 与随后读取的数据库记录完全一致。非法结束
  时间、倒退更新时间和 i64::MAX revision 明确报错，测试确认失败后 part/session 版本不变。
- 历史 fixture 保留 **17 个**原 part 触发器。重开回归先安装历史定义并复现四条可绕过的
  SQL，再由正式 initialize_schema 刷新，验证同样写入被拒绝、既存行和表/索引定义保留、
  user_version 仍为 **13**，再次重开不改变 schema cookie。声明比较另确认只有 **5 个新增、
  3 个修改、0 个删除**的 part 触发器；表/索引声明与 HEAD 一致，并在 SQLite 3.51.0 中成功
  执行，见 `parts-declaration-diff.json`。未新增兼容层或放宽旧字段/schema 拒绝规则。
- 工作区全部 targets Clippy（`-D warnings`）、Rust 格式、19 项失败语义不变量和 diff 检查
  通过，最终驱动退出码 0。记录为 `parts-integrity-verification.json`，验证前后源码哈希均为
  `67cb0e518ccea95d40609824a0deb7103cb3e0fe7758fdedf1ea22707c3d14f3`，之后才更新开发/审查文档。
  测试仅访问内存或临时数据库；安装服务 PID **54968** 再次确认仍运行，没有提交或部署。
  Web、实际文件 HTTP、插件额外功能矩阵和浏览器保留前述检查点证据，本轮未重复这些检查。

本轮保证的是上述语法/字段约束和更新准备阶段的失败保留。完整 part/Failure 类型的读取、
历史坏行识别、JSON 数值/深度等解析边界、完整状态转换图和重复/冲突 complete_run 的语义
仍需继续审查。内存后端的 lease、membership、session version 等跨锁操作尚未据此证明与
SQLite 事务完全等价；其他批量写入、导入和并发失败路径保持开放。本机 macOS 成功也不替代
Linux/Windows、远端 CI 或可见页面验证。

## 全局插件注册的精确所有权（2026-09-06 12:53）

- 最终工作区日志汇总 **2,513 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture**
  通过。插件包默认配置 **143 项**、signing **148 项**、WASM **147 项**、all-features
  **152 项**分别通过；这些功能配置重复覆盖基础用例，不合计成不同测试。包含既有的
  **23 项完整 PluginHost 真实子进程回归**和 **15 项 stdio 生命周期回归**。
- 本轮新增 **17 项**回归，连续 **20 个独立测试进程**全部通过，驱动逐轮核对实际数量，见
  `plugin-ownership-repeats.json`。其中宿主回调竞态用例每轮交错 60 次替换/旧进程清理；
  另用通知与通道明确暂停已经取出执行的工具 disposer，验证旧 handle 无法撤回回调时，
  仍由身份检查保护新值。这项确定性测试在新公共逻辑中构造暂停点，没有生产测试钩子。
- `plugin-ownership-before.log` 为 **10 失败 / 0 通过**：三类全局注册被旧所有者清理删除、
  三类快照误带其他所有者的值、三类显式删除留下原所有者的活动 effect，以及旧 manifest
  清理删除后继工具。替代值特意使用相同内容，避免把值相等误当作注册身份。另一个修复前日志
  `plugin-remove-listener-before.log` 为 **1 失败 / 0 通过**，用 try_read 及时失败来验证
  删除监听器的持锁问题，没有让测试刻意永久阻塞。
- 工具注册表在同一写锁内保存值和所有权；display/theme 共用专用贡献注册表。独立注册身份
  用于清理比较，弱作用域引用用于实际归属与精确 handle 释放。替换成功后退休真正的旧
  所有者，失败仍保留原值；已经开始的旧清理回调不能删除新值或推进工具目录版本。重复注册、
  A→B→A 归属转移、根/子与兄弟作用域交错、其他插件同名键、过期实际回调均有回归。
- manifest 工具逐项归属到逻辑作用域，跳过初始化或交接时已由进程接管的替代项；重启恢复
  的默认工具重新归属到逻辑作用域。快照直接读取当前所有权，工具、显示和主题均只转交目标
  进程的条目。显式全局删除保持同插件内的既有语义，但释放实际所有者；克隆工具表作为独立
  值快照，修改副本不会退休原表的 effect。工具删除事件现在也在释放注册表锁后通知监听器。
- 工作区全部 targets Clippy、插件 all-features 全部 targets Clippy（均 `-D warnings`）、
  Rust 格式、19 项失败语义不变量和 diff 检查通过，驱动退出码 0。记录为
  `plugin-ownership-verification.json`；验证前后源码哈希均为
  `7f95f0eff406fd61b0f487eae7bf2e2df6e20426235a28a7aa0b77d574a4ecce`，随后才更新文档。
  工作区 debug 示例链接仍有既存的 macOS compact-unwind 体积警告；未为消除此警告禁用
  unwind。安装服务 PID **54968** 经复核仍运行，没有提交、部署或改动个人数据库。

这次验证覆盖全局工具/显示/主题的登记、清理和快照归属，不代表插件宿主所有资源都已有
相同保证。当时开放的 hook/transport 登记、整体回退清理的 current-scope/await 竞态和
首次作用域并发创建，在 14:32 检查点继续处理；长时间重启后的作用域历史增长和任意异步
清理器的期限仍开放。通用 ScopedRegistry 的准入/发布顺序在下一检查点继续处理。快照在真实 stdio 交接中
先关闭目标进程的准入；这不是跨所有贡献表或整个宿主的原子事务。Web、文件实际 HTTP、
浏览器、dist 独立可执行文件与其他平台保留前述检查点证据，本轮没有重跑这些检查。

## ScopedRegistry 的登记与清理原子性（2026-09-06 13:14）

- 最终工作区日志汇总 **2,523 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture**
  通过。插件包默认配置 **153 项**、signing **158 项**、WASM **157 项**、all-features
  **162 项**分别通过；包括既有完整宿主与真实 stdio 子进程回归，各配置共享的基础用例不
  合计为不同测试。本轮新增 **10 项**回归，连续 **20 个独立测试进程**全部通过，逐轮核对
  实际数量，见 `scoped-registry-repeats.json`。
- `scoped-registry-before.log` 为 **7 失败 / 0 通过**。两个登记入口先暴露尚无清理 handle
  的 candidate，再因所有者已停止准入而报错；并发清理可能让登记返回成功但条目已经不存在，
  或让显式删除无法释放后来才创建的 effect。失败替换的恢复分支还把已经成功写入的
  `(generation 3, successor)` 覆盖为旧的 `(generation 1, original)`。另验证饱和代次
  被继续接受，以及 256 次无效自循环留下 256 个作用域记录。
- 观察与暂停使用泛型 key 的 Clone 扩展点：只在注册表锁可获取时观察状态或等待通道，
  没有生产测试钩子和概率性睡眠。两个拒绝用例通过该扩展点观察短暂公开的值；另外三个竞态
  在独立阻塞线程中登记，由主测试线程执行关闭、清理、后继登记或显式删除。修复后的暂停点
  位于修改之前，验证相同交错能够保留后继值或得到一致的串行顺序。
- `register_with_effect_kind` 和 `replace_owned` 现在共用登记实现，在同一注册表锁内
  完成准入、发布和旧 handle 退休。条目的 effect 从可选字段改为必需字段，移除事后补充
  handle 和失败恢复分支；泛型 key 的副本在锁外准备。重复键、所有者关闭及注册代次耗尽
  都发生在可见修改之前，保留原值、父子关系、known_scopes 和 next_generation。
- 所有者比较使用精确弱作用域身份，同一插件的兄弟或后继作用域仍不能替换/删除别人的
  scoped 条目；owned_entries 也按相同身份选择。注册代次只在 checked_add 成功时推进，
  无法继续推进则返回明确的 GenerationExhausted。循环父子关系在登记元数据之前拒绝。
  全局层与 overlay 层均保留既有重复隔离、同所有者更新和祖先回退语义。
- 正常并发回归分别在全局和会话层执行 16 个同时开始的替换，逐个核对返回代次、被替换值
  与最终值之间的连续对应关系，确认只有一个活动清理器。随后执行全部旧手工句柄的清理，
  最终条目及其归属继续保留。另覆盖同插件/不同插件的所有者隔离，以及代次边界拒绝后仍可
  正常清理已存在条目；代次耗尽验证使用受控测试状态，不表示现实中已经运行到 u64 极限。
- 工作区和插件 all-features 的全部 targets Clippy（均 `-D warnings`）、Rust 格式、
  **19 项失败语义不变量**和 diff 检查通过，驱动退出码 0。记录为
  `scoped-registry-verification.json`；验证前后源码哈希均为
  `50f4ee7dbf3c747c91982eaa02792b4275f343688c59f7c762bdbe429191f0dd`，随后才更新文档。
  安装服务 PID **54968** 经复核仍运行；没有提交、部署或修改个人数据库。Web、文件实际
  HTTP、浏览器、dist 独立可执行文件与其他平台保留此前证据，本轮没有重跑这些检查。

本轮保证单个 ScopedRegistry 的上述操作边界。clear_scope_tree 清除的是当时的子树，后续
仍可重新登记该层；它不是永久关闭或会话授权撤销。hook/transport 登记、回退清理与实例
切换的上述竞态及首次根作用域创建在下一检查点继续处理。跨注册表工具目录版本/事件的并发
时序、正常使用中的元数据和 effect 历史增长，以及进程交接整体事务仍需继续审查。此次
没有改变数据库 schema、旧字段拒绝规则或插件作用域的重复隔离策略。

## hook/transport 所有权与宿主回退清理（2026-09-06 14:32）

- 最终工作区日志汇总 **2,534 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture**
  通过。插件包默认配置 **164 项**、signing **169 项**、WASM **168 项**、all-features
  **173 项**分别通过；各配置共享的基础用例不合计为不同测试。新增 **11 项**回归连续
  **20 个独立测试进程**全部通过，逐轮核对数量，见 `host-lifecycle-repeats.json`。
- `host-lifecycle-before.log` 为 **7 失败 / 0 通过**：旧 hook 和 transport 清理删除后继、
  两种关闭后登记仍修改可见状态、分别等待两个异步锁的回退清理误删后继，以及并发首次
  使用产生多个根作用域。另一个 `host-fallback-cancellation-before.log` 为 **1 失败 /
  0 通过**，取消等待 service-binding 锁时 callback token 已被提前删除。
- 回退竞态测试先完成 scope.dispose，再持有目标异步锁，手动将清理 future 轮询到 Pending，
  然后发布后继或取消等待，没有生产测试钩子和概率性睡眠。并发首次使用在每个测试进程执行
  **32 轮、每轮 16 个线程**的 barrier 同步启动。正向用例还覆盖正式 service 调用继续路由
  到后继、相同 transport Arc 和相同 hook 内容重复登记的独立归属，以及重复回退只清理
  当前插件并保留其他插件的目录、路由、索引、token 和服务绑定。
- hook_catalog 和 plugin_transports 共用 ContributionRegistry，每个登记携带精确身份
  与清理 handle。关闭准入时两个 API 返回 HostUnavailable 并保留前值；构建将 manifest
  所有权、hook 和 transport 登记放在同一个可失败阶段，全部成功后才公布激活。失败关闭
  transport 并回滚对应 scope，保留 effect_scope_registration_failed 分类。直接 API
  拒绝行为已有测试，本轮未新增专门在第二/第三登记阶段注入失败的完整构建测试。
- 贡献表按插件键再按条目标签嵌套，transport 查找可借用键，避免每个流事件新建组合查询键；
  保留原有暴露顺序。调用前取出 Arc 并释放短同步锁，任何 transport await 都不持有此锁。
  此处没有性能基准结论。首次根作用域创建由一把写锁内的 entry 完成，保留进程 task-local
  所有权及关闭后不隐式重建逻辑作用域的规则。
- 回退清理先取得 token mutex 和 service-binding 写锁，再核对当前 scope 的精确指针身份，
  保持代次读锁直到同步清理结束。实例切换不能穿过该段，等待异步锁期间取消也不会先删除
  部分回退元数据；重试可以完成清理。scope.dispose 已先执行，不能把此保证理解为取消后
  恢复原插件，或所有 shutdown 阶段都是原子操作。移除路由绑定也不会自行调用 transport.close。
- 最终工作区与插件 all-features 全部 targets Clippy（均 `-D warnings`）、Rust 格式、
  **19 项失败语义不变量**及 diff 检查通过，验证驱动退出码 0。首次验证被不再使用的私有
  replace_async 的 dead-code 诊断阻止；移除该遗留方法后重新验证，原记录保存在
  `host-lifecycle-validation-1/`。已有拒绝替换回归继续检查同步/异步旧 disposer 都会执行。
  `host-lifecycle-focused-2.log` 的缺少测试类型导入属于测试编译问题，修正后 focused-3
  的 **55 项 host 测试**通过，不计为产品缺陷。
- 最终记录为 `host-lifecycle-verification.json`，验证前后源码哈希均为
  `62fd58f31b8c191a483b134b2200989fff431ef9b125861f29c5039339d2ea6f`，随后才更新文档。
  安装服务 PID **54968** 经复核仍运行；没有提交、部署或修改个人数据库。Web、文件实际
  HTTP、浏览器、dist 独立可执行文件、远端 CI 和其他平台沿用此前证据，本轮没有重跑。

当时开放的进程内 ScopedHostClient 准入和在途生命周期在下一检查点继续处理。HTTP
callback 凭据按代次轮换、各 transport 的实际回调能力与复用、跨注册表目录事件时序及快照
一致性、scope/effect 历史增长及多插件整体重载事务仍保持开放。本轮的精确登记和回退
清理边界不能替代这些检查。

## ScopedHostClient 回调准入与在途清理（2026-09-06 14:48）

- 最终工作区日志汇总 **2,546 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture**
  通过。插件包默认配置 **176 项**、signing **181 项**、WASM **180 项**、all-features
  **185 项**分别通过。各配置重复覆盖的基础用例不合计为不同测试；既有完整宿主和真实
  stdio 子进程回归继续通过。新增 **12 项**回归连续 **20 个独立测试进程**全部通过，
  驱动逐轮核对数量，见 `scoped-callback-repeats.json`。
- `scoped-callback-before.log` 为 **7 失败 / 0 通过**。quiesce 只关闭 accepting，旧
  ScopedHostClient 仅检查生命周期 Active，因此仍读取配置和目录、写入通知并记录日志。
  被阻塞的回调没有活动 lease；作用域 dispose 或 quiesce 后，无论阻塞在宿主调用内还是
  host-client 读锁上，回调都继续 Pending。另在宿主操作内显式切换逻辑实例后登记主题，
  原回调的登记错误地属于后继。这是受控宿主实现中的重入测试，不声称复现整个 runtime 重载。
- **58 个** ScopedHostClient 方法共用 run_callback，在代次读锁内核查精确 scope 身份并
  获取 lease，之后才轮询实际操作。停止或过期的 Result 接口返回 HostUnavailable；没有
  返回值的 log 丢弃调用。取消优先于继续轮询宿主 future，lease 覆盖异步锁等待和已开始的
  回调，直到 future 完成或被丢弃，避免清理先于回调退出。
- 测试手动将 future 轮询到 Pending，用通知控制完成、取消令牌的 drop guard 观察释放，
  disposer 检查回调已释放后才清理，不依赖概率性睡眠。正向用例覆盖正常完成、调用者取消、
  旧 client 被拒绝而后继继续可用，以及宿主回调请求自身 scope.dispose 时不会等待自己的
  lease。后者验证回调取消与独立清理 worker 的配合，不等于完整重载事务或外部副作用回滚。
- 原 PROCESS_EFFECT_OWNER 改为统一回调所有权绑定，同时保存 scope 和 HostHandle 的
  精确弱引用。进程内回调登记保留准入时的所有者；stdio 回调和交接导入继续保留进程子
  作用域。其他宿主的同名插件不能借用这个 owner，退出绑定后普通登记继续归属当前作用域。
  相应回归验证原作用域清理只删除自己的登记，以及跨宿主条目的独立清理。
- 原有宿主签发的 session/workspace/authority 上下文继续传递；过期凭据在转发的配置调用
  上被拒绝，且不会进入内部 HostClient。此结果不能推广到全部本地目录方法的授权校验，
  当时开放的上下文准入差异在 15:11 检查点继续处理。已有方法转发内容经逐项核对；未改变插件 HostClient
  trait 的接口或数据库兼容规则。
- 工作区和插件 all-features 全部 targets Clippy（均 `-D warnings`）、Rust 格式、
  **19 项失败语义不变量**与 diff 检查通过，验证驱动退出码 0。最终记录为
  `scoped-callback-verification.json`，验证前后源码哈希均为
  `6a6e6e6e768f84394e4c04d97624ba05b292be090cfb09db4c7fc93bbe691374`，随后才更新文档。
  安装服务 PID **54968** 经复核仍运行，没有提交、部署或修改个人数据库。Web、文件实际
  HTTP、浏览器、dist 独立可执行文件、远端 CI 与其他平台保留此前证据，本轮没有重跑。

本轮边界是使用 ScopedHostClient 的回调 future。已经完成的写入、另行启动的后台/阻塞任务、
返回的订阅和活动 adapter 不会因丢弃该 future 自动撤销。HTTP bridge 仍先校验 token，再
进入 stream/router，尚未证明这两个步骤之间的停止与实例切换具有相同保证。当前 WASM
实现链接 WASI 并导出 alloc/dispatch，没有安装直接 HostClient import；cdylib 也未覆盖
attach_host。它们的实际回调能力和复用需要按各自实现检查，不能视为已走过此进程内路径。

## 回调授权与上下文隔离（2026-09-06 15:11）

- 最终工作区日志汇总 **2,560 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture**
  通过。插件包默认配置 **190 项**、signing **195 项**、WASM **194 项**、all-features
  **199 项**分别通过；SDK 默认功能配置单独运行，为 **136 passed / 0 failed / 3 ignored**，
  也有 **27 项宏编译 fixture**通过。重复功能配置与 fixture 不合计为不同测试。本轮新增
  **14 项**回归连续 **20 个独立测试进程**通过，逐轮核对数量，见 `callback-authority-repeats.json`。
- `scoped-authority-before.log` 为 **7 失败 / 0 通过**：未签发 session_id 的登记、更新、
  删除和目录读取都成功；过期 authority 能删除主题，其他插件的 authority 可发通知，
  把合法 token 的 session 17 改为 18 仍能更新会话 18 的工具。测试通过实际 scoped
  HostClient 方法操作内存宿主，没有伪造校验函数的返回值。它证明宿主 API 契约存在缺口，
  不把进程内插件代码描述为独立的安全沙箱，也不声称已复现外部 HTTP 攻击。
- 所有 58 个 scoped 方法共用的入口现在先校验和归属上下文，再轮询实际操作，覆盖不经过
  内部 HostClient 的工具/目录/主题/通知方法。错误返回 PolicyDenied，快照验证拒绝前后
  全局与两个会话目录、事件版本、主题、通知、effect 记录及活动 lease 数均不变。正常
  session 授权仍可完整登记、读取、更新、删除，并与另一个会话隔离；无特权全局登记仍可用，
  plugin_id 由绑定 client 确定。
- `callback-context-before.log` 为 **3 失败 / 0 通过**。raw RPC 虽然验证了 plugin-only
  上下文，随后却通过补丁函数重新继承外层 session 17，读取到了该会话工具；新签发的
  call_id 42 上下文混入外层 session/workspace/tool 字段，使自己的 token 校验失败。
  格式错误的 context 在目录读取上只记录警告并继续，错误被降成全局请求。
- SDK 新增 run_in_isolated_host_callback_context，完整设置给定上下文并在返回、Pending
  或取消时恢复外层状态。宿主签发 authority 的公共调用包装、原生流调用与模拟流回退、
  scoped 回调入口和 raw RPC 验证入口使用它。既有 run_in_host_callback_context 继续
  作为字段补丁使用，未整体改变需要继承上下文的内部调度语义。新 authority 的回调校验、
  显式 RPC authority 对全部外层特权字段的覆盖，以及取消调用后 token 撤销和外层恢复，
  均有回归。
- raw RPC 的 context 解码现在传递错误，且非 null 值必须为 JSON 对象，避免 Serde 将
  空数组视为空结构体。两个目录方法分别验证标量、数组和错误字段类型；缺省、null、空
  对象仍按插件级上下文执行并清除外层特权。没有新增旧字段兼容桥或改变数据库 schema。
  该检查点尚未覆盖 HTTP bridge 在 raw router 之前处理的 stream-event 分支，后由 16:01
  检查点补充实际接收路径验证。
- `callback-authority-focused.log` 中一处失败是测试直接索引被省略的空 tools 数组，已改为
  按正式响应类型解码；另一处则揭示上述空数组解码问题，补上对象检查。最终 focused-3 的
  **81 项 host 测试**通过。工作区和插件 all-features 全部 targets Clippy（均
  `-D warnings`）、Rust 格式、**19 项失败语义不变量**与 diff 检查也通过，完整驱动退出码 0。
- 最终记录为 `callback-authority-verification.json`，验证前后源码哈希均为
  `eb5450c37031d52848d172600f21f72607b2886cade22acbf5e3c0b8b3beb07b`，随后才更新文档。
  安装服务 PID **54968** 经复核仍运行；没有提交、部署或修改个人数据库。Web、文件实际
  HTTP、浏览器、dist 独立可执行文件、远端 CI 和其他平台保留此前证据，本轮没有重跑。

本轮验证上下文在这些 API 边界上的真实性、隔离与拒绝保留，不代表所有 capability 参数的
权限策略或凭据生命周期均已审计。HTTP bearer 与精确作用域绑定、认证到路由之间的切换、
stream-event 授权及回调取消在该检查点仍开放。SDK HTTP 流事件上下文丢失随后于 15:36
修复，宿主的流授权与调用绑定于 16:01 继续处理。复用 HTTP transport 后如何向插件传递
新 bearer 仍需单独验证，不能只轮换宿主 token 而忽略协议另一端。完成写入、分离后台任务
和多插件重载事务的既有边界继续保留。

## SDK HTTP 流回调与请求上下文（2026-09-06 15:36）

- 最终工作区为 **2,566 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture**。
  插件宿主默认/signing/WASM/all-features 分别为 **190 / 195 / 194 / 199 项通过**。
  SDK 独立默认配置 **136 passed / 3 ignored**、HTTP 配置 **143 passed / 3 ignored**、
  all-features **144 passed / 4 ignored**，三者均为 0 failed，并分别通过 27 项宏 fixture。
  重复功能配置不合计为不同测试。
- `http-stream-context-before.log` 为 **4 失败 / 0 通过**。真实 SDK HTTP router 向
  loopback callback recorder 发出的配置回调带有完整授权，而 chunk 和成功、插件错误、
  插件任务 panic 后的 terminal 包均丢失 plugin_id/authority_token；两个并发调用也如此。
  `http-request-context-before.log` 另有 **2 失败 / 0 通过**，用真实 Axum middleware
  设置外层上下文，普通和流请求在省略 Request.context 时借用了 test-authority-999。
- HTTP driver 从保存的请求上下文取得 plugin_id 和 authority_token，在进入分发前构造
  流上下文，供插件任务、chunk 转发及全部 terminal 分支使用。session/call/workspace/tool
  仍来自 ToolInvokeInput，与已有 SDK 行为一致。两条 HTTP 分发均完整设置上下文，缺省
  请求不会继承中间件的权限字段；不改变内部需要继承字段的补丁辅助函数。
- **6 项新增回归**连续 **20 个独立进程**通过，逐轮核对数量，见
  `http-stream-context-repeats.json`。每个 fixture 启动实际 SDK HTTP 服务和回调接收
  服务，经 meta/init 安装固定测试 bearer，核对实际 Authorization header、完整 context、
  stream_id、配置/数据块/terminal 顺序及两条并发流的各自授权。panic 用例实际触发 SDK
  spawned task 的终态接收失败分支，没有生产测试钩子或概率性睡眠。
- 工作区和插件宿主 all-features 的全部 targets Clippy（均 `-D warnings`）、Rust 格式、
  **19 项失败语义不变量**、diff 检查通过，14 项检查的完整驱动退出码 0。最终证据为
  `http-stream-context-verification.json`，UTC 完成时间为 `2026-09-06T07:36:25.741588+00:00`，
  验证前后源码哈希均为 `80eb922935b7694691b299a5b84dfd3e36c9008f64720d115c93a6d0543da3ea`，
  构建没有改变受跟踪源码，随后才更新文档。安装服务 PID **54968** 和 **3210** 监听
  经复核仍在；没有提交、部署或修改个人数据库。

本检查点的接收器只记录包并返回成功，不运行真实 runtime bridge 或验证 authority。
因此该检查点只完成 SDK 发送侧传播与独立请求隔离；宿主 stream-event 授权与具体 stream_id
的关联由下一检查点处理。HTTP bearer 生命周期/精确作用域绑定及复用交接继续开放。未新增
对 ToolInvokeInput 与 Request.context 特权字段不一致的 SDK 拒绝规则。Web、文件实际
HTTP、浏览器、dist 独立可执行文件、远端 CI 和其他平台沿用各自此前证据，本轮没有重跑。

## HTTP 流接收授权、注册与终态保留（2026-09-06 16:01）

- 最终工作区 **2,584 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture**。
  插件宿主默认/signing/WASM/all-features 分别 **200 / 205 / 204 / 209 项通过**；
  运行时插件包默认与 all-features 分别 **12 项通过**，均为 0 failed / 0 ignored。
  功能配置中的重复测试不合计为不同测试；SDK 独立功能矩阵沿用 15:36 的证据。
- `http-stream-ingress-before-2.log` 明确复现 **5 失败 / 2 通过**。测试启动实际 SDK HTTP
  插件和调用真实 dispatch_plugin_rpc 的 Axum 回调服务，经完整 PluginHost 初始化取得
  bearer 和宿主签发的调用授权。27 个无上下文/格式错误/无 token/伪造或修改 token 的
  请求中部分被错误接受；另一活跃调用的授权能注入并终止目标流；完成后的授权也被接受。
  两个被保留的成功用例验证正常插件错误与 SDK spawned task panic 的终态传递。
- 同一复现日志还证明数据保留问题：使用测试 HTTP middleware 暂停最初的 stream_id
  响应，使 SDK 实际发送的 chunk 和 terminal 全部先到达接收端。**64 块 + end** 得到
  空 chunk 列表；**128 块 + end** 把已发生的溢出覆盖成 complete 成功结果。
- 流事件入口先执行宿主已有的短期 authority 校验，再进入 transport；无授权为 PolicyDenied，
  非对象或不能解码的 context 被拒绝。独立 HostHandle 回归用记录 transport 证明未签发、
  过期和其他插件的授权在分发前被拒绝，并证明 transport 只观察经过校验的独立上下文。
  HTTP transport 进一步比较完整请求上下文，使另一个仍合法的调用也不能写入目标流。
- invoke_stream 在发出 HTTP 请求前登记 pending 调用。guard 在取消、超时或错误返回时
  同步撤销该记录及其早到包。最多 **128 个 pending 调用**；只有匹配的调用授权能预留
  一个早到 stream_id，最终响应必须一致，重复活动/预留 ID 被拒绝并保留原消费者。
  不再为未知、完成或取消的流分配无主缓冲。
- 新流注册、早到回放和后续交付共用一个同步状态锁，没有跨 await 持锁。每个 pending
  调用保存 **64 块 + 独立 terminal 槽**；首个终态保持不变，溢出不会被后续成功覆盖。
  活动消费者超过 64 块时仍明确失败。transport close 清空 pending 并结束活动流；
  接收器丢弃后的 monitor 只清理对应通道身份，避免误删重用同一 ID 的新流。
- **12 项宿主重点回归**与 **8 项实际 HTTP/bridge 回归**分别连续 **20 个独立进程**
  通过，见 `http-stream-ingress-repeats.json`。其中 2 项宿主用例从旧 HTTP 模块迁移，
  本轮净新增 **18 项测试**。128 次真实初始 HTTP 调用在早到包被接收后逐一取消，迟到
  终态被拒绝，随后第 129 次调用仍能成功；另有重复 ID、关闭、待注册上下文、延迟 monitor
  和两线程注册/交付顺序回归。没有生产测试钩子或概率性睡眠。
- 第一版 fixture 使用了会拒绝 read_config 的 NoopHostClient，导致等待启动通知超时，
  记录保留为 `http-stream-ingress-fixture-initial.log`；随后改为明确返回测试配置的
  HostClient。`http-stream-ingress-before.log` 中另有 panic 文案和 65 块溢出的错误文案
  预期不匹配；正式 before-2 修正 fixture 预期并以 128 块复现实际溢出丢失。这些测试
  构造问题没有计入产品缺陷。最终 focused-2 的 8 项 HTTP 回归及 12 项宿主回归均通过。
- 工作区、插件宿主与运行时插件 all-features 的全部 targets Clippy（均 `-D warnings`）、
  Rust 格式、**19 项失败语义不变量**和 diff 检查通过，13 项检查的完整驱动退出码 0。
  `http-stream-ingress-verification.json` 记录 UTC 完成时间 `2026-09-06T08:01:41.078930+00:00`，
  验证前后源码哈希均为 `18faad5a36fe26c5897677395fdc41c6f67a58f87835fba110aac9a49f33279e`，
  构建没有修改受跟踪源码，随后才更新文档。测试增加两个已在锁文件内的 dev dependency，
  未更新第三方版本。安装服务 PID **54968** 与 **3210** 监听再次复核；没有提交或部署。

本轮实际 HTTP fixture 使用正式 runtime bridge，但把 bridge 错误包装成测试 JSON-RPC
错误响应，不覆盖应用的 ServerError/HTTP 状态映射。流授权与 stream_id 绑定已经验证；
bearer 的签发/校验与精确作用域准入、从认证到派发期间的代次切换、普通 HTTP 回调 lease
和在途取消，以及 HTTP 复用的凭据交接在此检查点仍开放，随后由 17:23 检查点处理。调用取消会撤销宿主 authority 和 pending
记录，不会停止远端插件已分离的工作；这不是远端取消协议。流数据仍需单独审查总字节预算。
Web、文件实际 HTTP、浏览器、dist 独立可执行文件、远端 CI 和其他平台沿用此前证据。

## HTTP 回调准入与复用交接（2026-09-06 17:23）

- 最终工作区 **2,616 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture**。
  SDK 独立默认/HTTP/全功能配置分别 **136 / 149 / 150 passed**，忽略项为 **3 / 3 / 4**。
  PluginHost 默认/签名/WASM/全功能配置分别 **210 / 215 / 214 / 219 passed**；
  runtime-plugins 默认和全功能配置各 **28 passed**。
- `http-handoff-before.log` 在实际 SDK HTTP router、完整 PluginHost 和正式 runtime bridge
  上复现 **2 失败 / 0 通过**：复用插件的配置回调使用旧 bearer；不同远端实例作为候选
  插件，在 init 中回调时 bridge 尚未发布新宿主，导致候选工具缺失。fixture 提供显式
  callback URL、宿主配置标记和可切换的 published-host selector。
- `http-handoff-expanded-before-3.log` 为 **12 通过 / 1 失败**，单独复现无人读取的流在
  交接后仍无法交付 terminal。更早 expanded-before 中另有 4 个 fixture 越界 panic：
  清理已经移除目录，而测试还从目录取第一个工具。修正为保留调用描述符，再验证停止的
  transport 拒绝调用；这些 fixture 问题及中间编译错误不计为产品缺陷。
- 凭据路由只在同一重载谱系内共享，保存签发 HostHandle 和精确作用域的 Weak 引用。
  准入在 token 锁下重新检查当前 scope 并取得 lease；lease 覆盖 inner HostClient 的锁
  等待和操作，作用域取消后先丢弃回调 future，再执行所有者清理。作用域替换后的旧凭据
  不能取得新作用域权限；旧清理保留新凭据，宿主销毁移除其路由。动态注册继续归准入时
  的 owner，即使操作中安装了 successor scope。
- SDK 增加 `meta/host.rebind` HTTP 控制方法，使用 expected/next 比较并替换回调目标，
  每次请求原子取得完整 URL/token。稳定代理转发全部 **58 个 HostClient 方法**，开启/
  关闭回调可在远端客户端和原本的本地 fallback 之间切换，无需重复 init。嵌套 binding
  必须为对象，未知字段和无 URL 的 token 被拒绝。控制操作不占普通的 64 个请求槽位。
  此为 protocol v2 的 HTTP 扩展，没有提升全局协议版本；不支持该方法的远端在停止
  前任前明确失败，不对同一外部实例静默再次 init。
- 宿主交接有 **30 秒**期限。先验证远端支持和所有权，再停止旧 scope、取消在途 HTTP
  请求及回调、打断活动流，转交工具、会话覆盖、display 和 theme，然后更新远端凭据。
  开始交接后的取消/失败同步关闭部分绑定的准入，并异步清理精确所有者。远端已更新但
  响应未返回时取消、交接中 close、清理失败、远端失败和过期前任争抢都有实际 HTTP 回归。
  这些失败会关闭本地绑定；已处置的前任不会被恢复为可用状态。
- 生命周期 wrapper 同时观察 terminal 与 chunk 转发。错误能够中断无人读取的满通道，
  成功终态仍等待排队 chunk 完整交付。此前 64 个早到 chunk、溢出错误、授权隔离与
  128 次取消后的容量恢复回归继续通过。
- 本轮净新增 **32 项测试**：宿主凭据/准入 **10 项**、SDK 交接 **6 项**、完整 HTTP/
  bridge 交接 **16 项**。重复矩阵含旧流回归：宿主 **22 项**、SDK **12 项**、runtime
  **24 项**各在 **20 个独立进程**通过，共 **1,160 次用例执行**；实际进程为 3 组各 20
  个，见 `http-handoff-repeats.json`。SDK 并发用例另含 256 次回调和 256 次交接，检查
  地址/bearer 组合不会混用。
- **16 项完整验证**全部退出 0，包括 workspace 测试、workspace `--all-targets` Clippy、
  三个修改包 `--all-targets --all-features` Clippy（均 `-D warnings`）、fmt、
  **19 项失败语义约束**和 `git diff --check`。工作区仍有此前记录的大型 macOS debug
  二进制 compact-unwind 链接提示，未为消除提示禁用异常展开。
- 证据目录为 `.tmp-artifacts/technical-debt-audit-2026-09-06/`。
  `http-handoff-verification.json` 的 UTC 完成时间是 `2026-09-06T09:23:03.017977+00:00`。
  包含受跟踪和非忽略未跟踪文件的验证前/后 SHA-256 均为
  `9bdf933b5d54a55ee192be03256b68333870d31201f31a6f1832b6655a6982bf`；
  tracked diff 的前/后 SHA-256 均为
  `492c29125cbba46e471ed9f1b808700035c5c8239fc884bdc480d857d06f30db`。
  验证期间未改源码/文档，随后仅更新开发和审查文档。未新增第三方依赖或修改版本。

应用组合边界仍需继续处理：`crates/agena-runtime/src/plugin_composition.rs` 当前传入
`callback_base_url: None` 和 `host_client: None`；运行时客户端在 PluginHost 构建后安装。
`apps/agena/src/server/app.rs` 在运行时 bootstrap 后才 bind/serve 公共监听端口。
运行时客户端的 read_config 仍读取 current_snapshot；fixture 使用的配置标记宿主不证明
应用候选配置已在发布前可用。生产 `/plugin-rpc/{plugin_id}` 还经过 UI auth middleware，
而正式 REST handler 的失败使用 ServerError；本轮未验证这两层 HTTP 行为。后续应运行
隔离的实际应用启动/重载用例，并设计首次初始化、监听地址、认证和候选服务绑定的接线。

更改配置或依赖 epoch 后针对同一外部 HTTP 实例创建新 transport 的 init/shutdown 所有权
在当时未由 unchanged-plugin 复用证明，随后由 18:07 检查点继续处理。跨多个插件的整体回滚、远端已分离工作、返回
资源生命周期、流总字节预算和历史增长仍开放。调用取消不撤销已经完成的写入。Web、
文件实际 HTTP、浏览器、dist 独立可执行文件、远端 CI 和其他平台沿用此前证据。
安装服务 PID **54968** 与 **3210** 监听保持正常，未提交、推送或部署。

## HTTP 远端实例替换、生命周期期限与响应关联（2026-09-06 18:07）

- 最终工作区 **2,641 passed / 0 failed / 5 ignored**，另有 **27 项宏编译 fixture**。
  SDK 独立默认/HTTP/全功能配置分别 **137 / 169 / 170 passed**，忽略项 **3 / 3 / 4**。
  PluginHost 默认/签名/WASM/全功能配置分别 **210 / 215 / 214 / 219 passed**；
  runtime-plugins 默认和全功能配置各 **33 passed**。
- `http-instance-replacement-before-2.log` 为 **1 失败 / 0 通过**。实际 SDK HTTP
  服务、完整 PluginHost 与正式 runtime bridge 中，将 settings 改为 `{"revision": 2}`
  后创建新 transport，初始化同一个远端插件。新工具在旧宿主 shutdown 前可用，之后
  报 `HTTP plugin instance has shut down`。最早的 replacement-before 使用 null 到
  空对象的变化，被配置归一化判为未改变；其中 init 次数断言属于 fixture 问题。
- 宿主初始化前要求 `meta/http.state` 返回对象、HTTP instance 扩展 **version 1**
  与可选 revision；全局 RPC 协议仍为 **2**。每个 HttpTransport 发送独立的
  `x-agena-plugin-instance`，初始化同时发送 `x-agena-expected-revision`，首次为 `-`。
  SDK 在生命周期锁下比较 revision，因此观察到相同 revision 的并发初始化只有一个
  成功。普通请求、shutdown、rebind 均验证当前 owner，迟到的旧 shutdown 无法关闭
  新实例。缺少该扩展或版本不兼容的旧 HTTP 实现会在 init 前被拒绝；这也是首次加载
  的兼容性要求，不能只描述为不支持热切换。
- 初始化在停止旧实例前验证对象形状、全局协议版本和 callback URL/token。
  InitContext 现在严格拒绝未知字段及已移除的 `config`。初始绑定与 rebind 共用
  HTTP(S) URL/token 校验；无 URL 的 token、空 token 和非法 header 被拒绝。
  实例 header 限单值、1–128 个 ASCII 字母/数字/`-_.`，重复、空、超长或非法值不能
  初始化或操作现有实例。这些值用于生命周期隔离，不替代 HTTP 端点认证。
- SDK 控制操作有独立 **30 秒**期限，覆盖锁等待、清理与插件 init/shutdown；宿主
  请求超时不再是唯一约束。取消或失败时守卫立即停止实例，并安排一次同样有 **30 秒**
  期限的串行清理，包含清理的锁等待。清理失败/超时保持停止状态，后续初始化必须先
  完成遗留清理。尚未取得生命周期锁的请求超时不会停止旧 owner，rebind 等待绑定锁
  超时也不会改动现有回调。
- 替换会取消 SDK 已接纳的普通请求、流转发和实际插件流 future，等这些操作释放后
  才调用 shutdown。新的 init 得到新的 HostClient 代理，旧实例保留的代理失效；
  同一实例的 rebind 仍保留稳定代理。64 条流占满普通派发容量时控制操作仍能完成，
  测试检查全部 64 个插件 future 已释放、容量恢复、后任工具可用。
- `http-instance-host-response-id-before.log` 和
  `http-instance-sdk-response-id-before.log` 各为 **1 失败 / 0 通过**。前者将真实
  state 响应改为其他请求 ID，宿主仍继续替换；后者返回不匹配 ID 的配置回调结果，
  SDK 仍报告成功。现已在两个 HTTP 方向的公共接收点验证 RequestId，修复后对应回归
  及既有 stream/context/handoff 用例全部通过。
- 本轮净新增 **25 项测试**：SDK 实例生命周期 **18 项**、回调响应 ID **1 项**、
  InitContext 旧字段/未知字段拒绝 **1 项**、实际 HTTP/runtime bridge **5 项**。
  重复矩阵含此前回归：宿主 **22 项**、SDK **31 项**、runtime **29 项**各在
  **20 个独立进程**通过，共 **60 个进程、1,640 次用例执行**，见
  `http-instance-repeats.json`。期限测试使用 Tokio 虚拟时钟，覆盖完整的 30 秒边界。
- **16 项完整验证**全部退出 0，包括 workspace 测试、workspace `--all-targets`
  Clippy、三个修改包 `--all-targets --all-features` Clippy（均 `-D warnings`）、
  fmt、**19 项失败语义约束**与 diff check。大型 macOS debug 二进制仍有此前记录的
  compact-unwind 链接提示，trybuild 的缩小依赖图也有 unused patch 提示；没有禁用
  异常展开或降低 Clippy 要求。没有新增第三方依赖版本，只启用已有 Tokio/tokio-util
  的 HTTP 生命周期功能和测试用虚拟时钟。
- 证据目录为 `.tmp-artifacts/technical-debt-audit-2026-09-06/`，
  `http-instance-verification.json` 完成时间为 `2026-09-06T10:07:56.562132+00:00`。
  含受跟踪及非忽略未跟踪文件的验证前/后 SHA-256 一致：
  `ad8a0b82a4509d7423591551a6cf068169627a2037045d3194f53dab42426f35`。
  tracked diff 前/后 SHA-256 一致：
  `9cfc45685b439b363d75b0124e78c89fadd4a82a83a1d55455c0e025b8227f13`。
  验证期间没有编辑源码/文档；随后仅更新开发与审查文档。

本轮隔离的是同一远端 Rust 插件对象上重新初始化的逻辑 owner，没有构造新的插件对象。
实际替换 fixture 的插件显式支持再次 init。源码中 `PluginSettings` 使用 `OnceLock`，
宏生成的 init 每次都会调用其 setter；含这种字段的插件是否需要 factory/reset 生命周期，
在当时仍需设计并补实际 HTTP 宏插件回归，随后由 18:45 工厂构造检查点处理。
shutdown 的取消或失败可能导致清理重试，插件清理代码也须能容忍再次执行。

生产 composition 的 callback URL、首次监听顺序、候选 HostClient/current_snapshot、
UI auth 与 REST 错误映射仍保持开放。本轮继续使用显式 callback URL 和标记 HostClient，
没有验证完整 AgenaRuntime 的发布/回滚。已停止的前任不会在新 init 失败后自动恢复；
多个插件整体回滚、外部独立宿主接管策略、分离/阻塞工作、返回资源生命周期、流总字节
预算与历史增长也仍需处理。生命周期取消不会撤销已完成的写入。
Web、文件实际 HTTP、浏览器、dist 独立可执行文件、远端 CI 和其他平台沿用此前证据。
安装服务 PID **54968** 与 **3210** 监听已只读复查，未停止服务、提交、推送或部署。

## HTTP 宏插件对象重建与释放（2026-09-06 18:45）

- 最终工作区 **2,647 passed / 0 failed / 5 ignored**，另有 **28 项宏编译 fixture**。
  SDK 默认/HTTP/全功能配置分别 **137 / 175 / 176 passed**，忽略项 **3 / 3 / 4**。
  PluginHost 默认/签名/WASM/全功能分别 **210 / 215 / 214 / 219 passed**；
  runtime-plugins 默认与全功能各 **33 passed**。
- `http-factory-before.log` 为 **2 失败 / 0 通过**。新建真实 SDK HTTP router，使用
  `#[agena_plugin(settings_field = settings)]` 和 `PluginSettings<Settings>`，首次
  初始化成功后更改 revision 再 init，或首次 init 返回错误后再次 init，均报
  `test.http-settings plugin config already initialized`。测试同时返回对象 ID 与
  配置值，保证修复后得到新对象和新配置，而非只绕过 OnceLock 的重复写入错误。
- `http::router` 现在接收 `Fn() -> P + Send + Sync + 'static` 工厂。直接调用方需将
  `router(MyPlugin::default(), host)` 改为 `router(MyPlugin::default, host)`，或提供
  构造新状态的闭包。SDK `export_http!` 与属性宏生成的 HTTP 导出入口自动在工厂内
  求值插件表达式。首次构造的对象提供固定 manifest 并用于首次 init，后续 init 另建
  对象；工厂 panic 或 manifest 改变会在停止旧实例之前被拒绝。
- dispatcher、callback client 与稳定 HostClient 代理改由精确实例持有。普通请求的
  admission 同时保留该 dispatcher，旧实例的清理始终指向原对象。rebind 保留当前
  对象及其配置，不调用工厂、不重复 init；真正替换则为新对象提供新的 OnceLock。
  原有 owner/revision 比较、迟到旧 shutdown 拒绝、请求取消、64 条流释放和生命周期
  期限回归继续通过。
- `http-factory-release-before-2.log` 为 **3 通过 / 2 失败**，进一步复现 shutdown
  成功后旧对象仍由 router 持有，以及不兼容 init outcome 被接受。现在完成 shutdown
  后释放 dispatcher/对象，保留 owner/revision 防止旧请求争抢；重复 shutdown 不重复
  调用清理。InitOutcome 的 manifest 和协议在 ready 前再次验证，不兼容结果停止
  该实例并进入清理。真实 HTTP 用例覆盖错误协议和改变 manifest 两种返回结果。
- 上述 release-before 的第一份日志同时包含一个 fixture Drop 的 PoisonError：断言
  失败时持有观察列表的 Mutex，之后测试析构器再次 unwrap。改为析构时恢复观察锁，
  再次运行仍得到同样两个产品失败，第二份日志没有这个附带 fixture 错误。
  首次 export 编译 fixture 缺少宏要求的 summary，补齐后通过；这也不计为产品缺陷。
- 新增 **5 项真实 HTTP 宏插件生命周期测试**和 **2 项工厂失败保留前任测试**。
  删除 **1 项**针对旧全局 callback-client 安装锁等待的测试：新实现同步构造实例内
  dispatcher/proxy/callback client，已经不存在那个 await 窗口；真正的 init 取消、
  cleanup 取消/重试、rebind 等待和期限测试保留。测试净增 **6 项**。新增 **1 项**
  HTTP settings 导出宏编译 fixture，HTTP 与全功能配置均实际编译该导出分支。
- 重复矩阵：宿主 **22 项**、SDK 库内 HTTP **32 项**、runtime **29 项**、SDK 实际
  HTTP 宏插件 **5 项**，每组均在 **20 个独立进程**通过，共 **80 个进程、1,760 次
  用例执行**，见 `http-factory-repeats.json`。宏插件组包含配置重载、失败后重试、
  shutdown 释放对象、不兼容 init outcome 与保留对象的 rebind。
- **16 项完整验证**全部成功，包括 workspace 测试与 `--all-targets` Clippy、三个
  相关包 `--all-targets --all-features` Clippy（均 `-D warnings`）、各功能组合、
  fmt、**19 项失败语义约束**及 diff check。构建仍有此前记录的 macOS 大型 debug
  compact-unwind 与 trybuild unused patch 提示，没有修改异常展开策略或放宽检查。
- 证据目录 `.tmp-artifacts/technical-debt-audit-2026-09-06/` 中的
  `http-factory-verification.json` 完成于 `2026-09-06T10:45:09.895560+00:00`。
  受跟踪及非忽略未跟踪源码的验证前/后 SHA-256 一致：
  `06d21a47ffcfdc5f78ee6b2f3245e8b5712fd2ce39f39e1b7eb21121b58385ac`。
  tracked diff 前/后 SHA-256 一致：
  `f9fe0ad4860682686c22d4239d2e0e45363c5d56f91bf0f23fc196685159d471`。
  验证期间未改源码/文档，之后只更新开发/审查记录。本轮未新增或升级依赖。

这解决了 SDK HTTP 宏插件共享同一 Rust 对象导致的配置重载/重试失败；工厂仍须提供
新的实例状态，不能把本应独立的配置或资源重新放进共用的 Arc。同步构造/析构和任意
阻塞代码不能被 Tokio 异步期限抢占，异步设置应放进 init；分离任务和返回资源的生命
周期仍需单独处理。清理失败保持对象用于重试，后任 init 失败不恢复已处置的前任。

在 18:45 检查点，生产 `compose_plugin_host` 尚未配置 callback URL 与初始 HostClient，
运行时客户端在 PluginHost 构建后安装，应用监听端口在 bootstrap 之后建立。下面的新
检查点处理了本机 HTTP 生产装配与候选配置；整体回滚和公共 REST 错误映射仍保持开放。
Web、文件实际 HTTP、浏览器、dist 独立执行程序、远端 CI 与其他平台沿用
此前证据。安装服务 PID **54968**、**3210** 监听保持原状，未提交、推送或部署。

## 生产运行时 HTTP 回调、候选配置与生命周期（2026-09-06 19:28）

- 最终工作区 **2,655 passed / 0 failed / 5 ignored**，另有 **28 项宏编译 fixture**。
  SDK 默认/HTTP/全功能配置分别 **137 / 175 / 176 passed**，忽略项 **3 / 3 / 4**；
  PluginHost 默认/签名/WASM/全功能分别 **211 / 216 / 215 / 220 passed**。
  runtime-plugins 默认与全功能各 **33 passed**；agena-runtime 默认与全功能
  各 **76 passed**，包含本轮新增的 **6 项**真实运行时回归。
- `runtime-callback-before.log` 为 **0 通过 / 2 产品失败**。使用临时工作区、显式配置路径、
  内存数据库、实际 SDK HTTP server 与 `AgenaRuntime::new`：要求 init 读取配置的插件
  没有完成初始化；停止维护任务、显式 shutdown 插件并释放普通运行时句柄后，保留的
  HostHandle 仍使 RuntimeProcessState 的 Weak 可以 upgrade。此前仅验证显式 bridge
  无法发现生产装配没有 callback URL/初始 HostClient，以及客户端和事件监听器的强引用环。
- 运行时在插件初始化前绑定独立的 `127.0.0.1:0` 回调监听器，同一重载谱系的快照共享它。
  `PluginCallbackDispatcher` 可以先于首个 PluginHost 建立，继续复用原有精确凭据/
  scope 准入逻辑；路由仅保存 Weak。新公开构造入口拒绝与前任不同的 dispatcher 谱系，
  原 `PluginHost::new` 自动选择原有或新谱系；新增 **1 项**宿主回归覆盖这一入口。
- 每个候选 HostClient 在 init 前持有该次构建的完整 ConfigResolution JSON，
  `read_config` 在首次启动、重建和 rebind 时读到所属候选配置。运行时依赖操作在服务
  未就绪时返回 HostUnavailable；弱绑定先于快照交换安装，generation 检查随交换就绪。
  发布后，这类操作使用当前进程服务；这不是每一代都保留完整不可变服务集。
- Runtime 的克隆句柄共享一个内部所有者，HostClient 只弱引用它，避免对临时外层 Arc
  降级造成客户端立即失效。工具目录监听器通过弱 LiveSignalHub 同步发送通知，不再
  捕获运行时或为每条通知创建独立任务。回归验证原外层 Arc 被释放但克隆句柄仍在时
  服务可用，以及普通句柄释放后，保留 HostHandle/client/listener 不再阻止运行时回收。
  维护循环仍要求正常 shutdown；测试还显式 shutdown 插件。活动调用、返回资源及
  任意分离任务的生命周期不由这项引用环修复一并保证。
- PluginHost 一完成就安装 snapshot cleanup guard，覆盖后续 provider/service 构建
  失败及取消。两项真实运行时回归在 provider.list 中报错或暂停，验证已 init 的 HTTP
  插件执行 shutdown、未发布的回调监听端口释放。进程可见 host slot 延后至成功发布，
  restart 回归在候选 provider.list 暂停时确认 runtime snapshot 和 host slot 仍指向前任。
  初始化的可失败 ownership audit 写入也移至维护任务启动前。
- 新增 **1 项实际 agena 应用进程测试**：开启 UI 密码时首次 HTTP init 仍可读取配置；
  管理 API 未登录返回 401；专用回调接受插件 bearer，拒绝缺失/错误/UI-session token；
  token 与 context 准入失败分别返回带匹配 request ID 的 JSON-RPC 401/400。
  通过登录后的管理 API 异步重载，配置更新且插件 init 次数仍为 1，旧 callback bearer
  失效。所有进程使用独立状态目录和随机本机端口，测试未访问安装服务。
- 六组重复验证：宿主 **23 项**、SDK **32 项**、runtime-plugins **29 项**、HTTP 宏
  插件 **5 项**、生产 runtime **6 项**、实际应用 **1 项**，每组 **20 个独立进程**，
  共 **120 个进程、1,920 次用例执行**，所有退出码和精确用例数核对通过。
- **19 项完整检查**成功：工作区测试和 Clippy、五个相关包全功能/全目标 Clippy
  （均 `-D warnings`）、各功能矩阵、真实应用、重复矩阵、fmt、**19 项失败语义约束**
  和 diff check。本轮为 runtime/application 的测试显式启用 SDK HTTP feature，没有
  新增外部 crate 或升级依赖。大型 macOS debug 链接及 trybuild 的已知提示仍保留；
  runtime 单元测试与已有大测试目标一样仅允许 linker_messages，没有禁用 unwind。
- 证据目录中的 `runtime-callback-verification.json` 完成于 `2026-09-06T11:28:48.796844+00:00`。
  受跟踪及非忽略未跟踪文件的验证前/后 SHA-256 一致：
  `799e3543c8c62ee306e3aa9035e3678ac792f212e9b3fd30a8021cdb0753b5ae`；tracked diff 前/后 SHA-256 一致：
  `aa250ebaa9e1de7a34f9787a56fd5ea6215a2e9c7e98b642779420a7fa8bfa85`。验证期间源码与文档保持不变；此后仅更新开发/审查文档，见
  `runtime-callback-post-docs.json`。

监听器目前向插件提供本机回环地址，跨机器插件需要额外的 bind/advertised URL 设计。
原公共 `/plugin-rpc` 路由的 UI 认证及旧 REST 错误映射未改；SDK 使用新专用地址。
新回调路由的畸形 body/提取器错误、慢请求和总缓冲预算仍需独立检查。
多插件整体回滚和 SessionManager 发布原子性保持开放：候选清理不能恢复已处置的前任。
此检查点只验证直接 runtime/管理 API 重载及 init 阶段拒绝。活动插件自重载的两种失败，
以及后续请求/状态任务语义，由下一个 **20:40 检查点**复现、修复并验证。
Web、文件 HTTP 探针、浏览器、dist、远端 CI 和其他平台沿用各自此前证据。已只读复查
安装服务 PID **54968** 与 **3210** 监听；未停止服务、提交、推送或部署。整个审查仍在进行。

## 插件发起重载的任务语义与调用生命周期（2026-09-06 20:40）

- 最终工作区 **2,671 passed / 0 failed / 5 ignored**，另有 **28 项宏编译 fixture**。
  SDK 默认/stdio/HTTP/全功能分别 **137 / 141 / 175 / 178 passed**，忽略项
  **3 / 3 / 3 / 4**；PluginHost 默认/签名/WASM/全功能分别
  **213 / 218 / 217 / 222 passed**。runtime-plugins 默认及全功能各 **33 passed**，
  agena-runtime 默认及全功能各 **88 passed**，bundled plugins **108 passed**。
- `self-reload-before.log` 为 **0 通过 / 2 产品失败**：真实 HTTP 工具调用同步
  `host.reload_config()` 时，其自身作用域交接取消在途请求，工具报 transport disconnected；
  bundled settings 已写配置后，重载构建继承旧调用上下文，provider.list 报 authority token
  unknown or expired。原有直接 runtime/管理 API 重载测试没有覆盖这些发起路径。
- 增加可选 SDK 能力 `request_config_reload` / `config_reload_status`，RPC 方法分别为
  `host/config.reload.request` / `host/config.reload.status`。接收结果为
  `{task_id, started}`；后续状态为 `{task_id, state:{status:...}}`，支持 running、succeeded、
  failed（包含 UserProblem）、cancelled。running 包含等待原调用完成；未知/过期任务 ID
  返回错误。同步 SDK 方法仍供其他宿主自行实现，Agena Runtime 在修改前明确拒绝并提供迁移提示。
- 宿主为发起的请求／响应调用持有完成信号，将它与 authority record 关联；已验证回调
  在新 Tokio 任务中恢复该信号，嵌套调用继续共享最外层信号。原调用返回、取消或 future
  被丢弃时释放信号；原生流持有信号直到 transport 终态转发。信号本身不持有插件或宿主，
  运行时任务在独立 Tokio 上下文中等待后执行重载，避免构建继承旧 callback authority。
- 同一个最外层调用重复提交得到相同的活动 task ID 和 `started: false`，包含真实
  HTTP → HostClient.invoke_tool → SessionManager → bundled settings 的嵌套路径。
  已接收任务在发起调用取消后继续；回归还验证先接收、保持原调用 pending，再 shutdown
  运行时时任务转为 Cancelled 且未发布新代次。并发准入与 shutdown 的全部交错尚未验证。
- 无会话/工具上下文的普通请求／响应 hook 也签发不增加权限字段的 authority。
  `self-reload-context-free-before.log` 复现此前没有 authority，修复后 shell hook 和
  chat.params 链式 hook 均可在返回后完成自重载。单向通知、插件自行创建的分离任务和
  返回资源的寿命仍有独立边界；无 originating host context 的后台回调只以本次回调寿命为信号。
- bundled settings 写入现在返回 `reload_task`，文本明确为 Runtime reload queued，
  保持已完成的 `reload` 报告为空。不能在同一个发起调用内轮询等待任务成功，否则等待的是
  自己尚未释放的信号。不同发起调用可以产生分别串行执行的任务；独立外部重载仍可能
  中断另一个活动调用。本次没有引入全局无中断或整次配置修改回滚保证。
- 新协议测试发现 Serde 带标签的无字段枚举即使配置 deny_unknown_fields 仍接受旧字段；
  `self-reload-sdk-stdio-before.log` 保存 **1 通过 / 1 失败**。将零字段状态改为空 struct
  variant，保持线上 JSON 形状，并验证额外 generation/config、不完整 Failed 和未知状态被拒绝。
  旧完成报告不能解码为接收结果。SDK HTTP 代理与 ScopedHostClient 均完整覆盖 **60 个方法**。
- 本轮新增 **12 项真实 runtime 回归、2 项宿主完成信号回归、2 项 SDK stdio 回调测试**；
  runtime callback 模块现有 **18 项**。覆盖原生 HTTP stream、嵌套设置调用、改变 settings
  后替换原对象、延迟旧宿主 shutdown、发起调用取消、运行时关闭、畸形输入拒绝及真实构建失败。
  实际 provider 构建失败记录 Failed，已发布 generation 不前进；没有据此宣称前任恢复。
  stdio 用例验证内存队列和 pending-response 编解码，并非实际 stdio 子进程自重载。
- 实际开启 UI 密码的 agena 应用测试增加 SDK 后台回调提交/状态查询，确认请求得到接收结果，
  随后读到真实 Succeeded 与新配置，未重复 init。它使用隔离状态目录和临时监听端口。
- 六组各 **20 个独立进程**重复验证：宿主 **25 项**、SDK **34 项**、runtime-plugins
  **29 项**、HTTP 宏插件 **5 项**、生产 runtime **18 项**、实际应用 **1 项**，共
  **120 个进程、2,240 次执行**。用例数取自各次构建的二进制清单；全部精确计数及退出码匹配。
- **21 项完整检查全部通过**：工作区测试和 Clippy、六个相关包全功能/全目标 Clippy
  （均 `-D warnings`）、上述功能矩阵与重复运行、fmt、**19 项失败语义约束**及 diff check。
  中间出现的测试 future 未 pin 和 Clippy single_match 是测试写法问题，最终检查已通过。
  本轮没有新增依赖或升级全局 RPC/HTTP instance 版本。既有大型 macOS debug 链接提示保留，
  没有重新禁用 unwind。
- `self-reload-verification.json` 完成于 `2026-09-06T12:40:43.045788+00:00`。
  验证前后所有受跟踪及非忽略未跟踪文件 SHA-256 均为
  `a016247b8b667e358e94e00c25e984ce2e22fd6495ccac7aa04e5ecfd8cd3339`；tracked diff SHA-256 均为
  `d446be82ae2da354f714a253235205f86985e0bf089ecde99ee007ff53da10bd`。
  验证期间源码保持不变，之后仅更新开发和审计文档；见 `self-reload-post-docs.json`。

整次快照回滚、SessionManager 发布原子性、跨机器 callback 地址和资源总预算继续开放。
本检查点记录的后台任务准入/关闭间隙及活动排队数量问题，已由后续 **22:28 检查点**
复现并修复；当时的源码观察保留在 `self-reload-next-observations.md`。
其他平台、远端 CI、浏览器、Web、文件 HTTP 探针及 dist 沿用各自此前证据。已只读确认
安装服务 PID **54968** 仍监听 **3210**；未停止服务、提交、推送或部署。整个审计保持进行中。

## 后台任务准入、关闭、容量与失败生命周期（2026-09-06 22:28）

- 最终工作区 **2,691 passed / 0 failed / 5 ignored**，另有 **28 项宏编译 fixture**。
  SDK 默认/stdio/HTTP/全功能分别 **139 / 144 / 177 / 181 passed**，忽略项 **3 / 3 / 3 / 4**；
  PluginHost 默认/签名/WASM/全功能分别 **213 / 218 / 217 / 222 passed**。
  runtime-plugins 默认及全功能各 **33 passed**，agena-runtime 默认及全功能各 **103 passed**，
  bundled plugins **108 passed**，Application **33 passed**。
  API 默认/仅 HTTP/无默认功能分别 **28 / 25 / 1 passed**，实际应用回调测试 **1 passed**。
- `background-tasks-before.log` 为 **5 通过 / 5 产品失败**：Runtime 的初次关闭检查与登记之间
  仍可接收任务；预先取消的任务仍调用工作工厂；异步 panic 留下 Running 和去重占用；
  start observer panic 中断已登记任务的启动；finish observer 在注册表锁内运行而不能重入。
  `background-tasks-focused.log` 随后为 **10 通过 / 1 产品失败**，补充复现维护 TaskControl
  在 shutdown 后仍可启动任务。最早一次 Cargo 命令误传两个筛选参数，不计为产品失败。
- 注册表现在以同一把锁关闭准入和登记任务，关闭标记不会重新打开；原 Runtime 初次检查仅为
  快速路径。缺少 Tokio executor 时在创建记录前拒绝。取消优先且延迟构造工作 future，
  已取消任务不会执行工厂中的同步代码。工厂/future panic 形成实际 Failed，私密诊断只写日志。
- 弱引用 finalizer 在正常完成、首轮 poll 前后 abort 和 executor teardown 时清理任务；
  terminal 状态、控制项、worker handle、去重索引和历史裁剪在同一锁内完成，异常退出也能释放
  后续任务容量。监听器回调及被替换监听器的 Drop 均在锁外，start/finish callback panic 被隔离。
  TaskControl 的准入检查/guard 登记互斥，关闭时在锁外释放 guard。
- **本轮新增的容量策略**是整个 Runtime 最多 **64 个活动受管理后台任务**，包含等待发起调用
  完成或等待 reload gate 的任务，目录刷新和市场任务也共享该上限。去重先于超载拒绝，
  因而满载时仍可返回既有任务；关闭后则拒绝准入。原历史裁剪目标仍是 **64 条总记录**，
  不是另外保留 64 条终态记录。数量上限不能替代总字节预算、按发起者公平性或维护保留名额。
- 真实 HTTP 插件容量回归先填满 64 个名额，确认拒绝且没有发布新代次，再释放一个名额并
  完成实际重载。它揭示 SDK HTTP/stdio 回调把 typed RPC error 重建为 Internal；
  `background-tasks-http-error-before.log` 为 **102 通过 / 1 产品失败**。共享
  `PluginError::from_rpc_error` 现在保留完整 kind、Failure ID、retry/recovery、公开问题和诊断；
  旧式/畸形数据仍是错误，原始 data 只进入诊断。容量错误使用 HostUnavailable、Backoff、Retry；
  `background-tasks-capacity-recovery-before.log` 单独复现了原默认 RestartRuntime 恢复建议不适合超载。
- API 的目录刷新服务原先先把准入错误转成字符串，再包装成内部失败；实际路由回归
  `background-tasks-catalog-api-before.log` 为 **0 通过 / 1 失败**，满载时返回 500 而非 503。
  `ModelCatalogRuntimeService` 现在直接返回 RuntimeBackgroundTaskControlError，移除
  仅保存消息的 ModelCatalogRefreshError。Application 统一投影错误，重载/目录刷新/市场入口
  共享准入语义：关闭、容量和缺少 executor 为 503；未知取消 ID 为 404，终态/不可取消为 409。
  实际重载/目录刷新路由验证满载和关闭两种状态的 problem envelope、Backoff 与零额外任务。
- 释放容量后的回归又复现了后置失败：快照已发布，但可选目录刷新无法入队，导致重载被标成
  Failed；`background-tasks-post-publication-before.log` 为 **102 通过 / 1 产品失败**。
  现在已发布重载保留 Succeeded，刷新拒绝记入 catalog last_failure；测试同时确认 generation 2
  和可观察的目录失败。没有据此宣称目录刷新成功、自动延后重试或前任快照恢复。
- 本轮新增 **13 项注册表/维护回归、2 项实际 Runtime 回归、3 项 SDK 回归、2 项 API 回归**，
  共 **20 项**；Runtime callback 模块现有 **20 项**。16 个 OS 线程同时争用准入/关闭的测试
  有意将 Tokio worker 首轮 poll 留到关闭之后；它验证登记/索引交错，不证明在途效果的原子性。
- API 无默认功能测试原本仍引用关闭的 REST 模块；仅 HTTP 组合的旧测试仍连接未启用的通知
  SSE 路由。已按实际 HTTP/WS/SSE 依赖条件编译测试及 live 模块，没有让部分功能组合假装存在
  被关闭的路由。相关编译失败与 **25 通过 / 1 失败**证据分别在
  `background-tasks-api-no-default-focused.log`、`background-tasks-api-http-only-focused.log`。
- 一次冻结验证在实际应用测试中收到 401，见 `background-tasks-application-fixture-failure/`。
  十次普通诊断重跑均通过，但增加启动等待后复现：日志显示测试尚未显式重载，插件已因启动
  目录刷新完成 transport 复用，测试保存的初代 bearer 已失效。隔离子进程关闭公共目录拉取后，
  同样延迟的验证通过；最终移除诊断等待并保留失败时的子进程日志。没有改变产品凭据规则或
  用偶然重跑成功抹去失败证据。另一次主动中止的早期验证已归档于
  `background-tasks-before-catalog-adapter-review/`，该中止不是产品测试失败。
- 七组各 **20 个独立进程**重复运行：宿主 **25 项**、SDK **37 项**、runtime-plugins **29 项**、
  HTTP 宏插件 **5 项**、生产 Runtime **39 项**、实际应用 **1 项**、API **2 项**，共
  **140 个进程、2,760 次执行**；全部精确计数与退出码匹配。
- **25 项完整检查全部通过**：工作区测试和 Clippy、8 个相关包全功能/全目标 Clippy
  （均 `-D warnings`）、上述功能矩阵及重复运行、fmt、**19 项失败语义约束**和 diff check。
  未新增依赖，SDK protocol 仍为 2、HTTP instance extension 仍为 1。既有大型 macOS debug
  链接提示保留，没有禁用 unwind。
- `background-tasks-verification.json` 完成于 `2026-09-06T14:28:21.108025+00:00`。
  验证前后受跟踪及非忽略未跟踪文件 SHA-256 均为
  `564842b61935db71a1b628643229ce5849d315938fc21ac22bc2136401804c4e`；tracked diff SHA-256 均为
  `da3f32e2c64b01b28c47679ce3b6bccecc1eeedc4b1bf4b348ac7b2fc43686fe`。之后仅更新开发和审计文档，见 `background-tasks-post-docs.json`。

本检查点之后，直接 reload 的关闭/发布顺序及 SessionManager 提前重配由 **23:36 检查点**
复现并处理；多插件整体回滚、目录状态并发更新及会话持久化竞态仍需独立审查。同步 observer 仍可能长期阻塞；
取消不会撤销已完成的写入/发布，也不会自动停止任意阻塞、分离或远端工作。目录刷新没有自动延后
重试；实际 stdio 子进程自重载、远端 callback 地址、body/stream 总预算和其他平台仍然开放。
新边界见 `background-tasks-next-observations.md`。已只读确认安装服务 PID **54968** 仍监听
**3210**；未停止服务、提交、推送或部署。整个审计保持进行中。

## 重载与关闭顺序、候选会话发布及取消语义（2026-09-06 23:36）

- 最终工作区 **2,702 passed / 0 failed / 5 ignored**，另有 **28 项宏编译 fixture**。
  agena-runtime 默认/全功能各 **111 / 111 passed**，Application **35 passed**，
  API 默认/仅 HTTP/无默认功能分别 **29 / 26 / 1 passed**。
  SDK 默认/stdio/HTTP/全功能分别 **139 / 144 / 177 / 181 passed**，忽略项 **3 / 3 / 3 / 4**；
  PluginHost 默认/签名/WASM/全功能分别 **213 / 218 / 217 / 222 passed**。
  runtime-plugins 默认/全功能各 **33 / 33 passed**，bundled plugins **108 passed**，
  实际 Agena 子进程的 HTTP 插件回调测试 **1 passed**。
- `reload-shutdown-before.log` 为 **0 通过 / 3 产品失败**：关闭后直接 reload 仍成功发布 generation 2；
  等待 ReloadGate 的直接 reload 不被关闭唤醒；候选 provider composition 阻塞时，关闭不取消构建。
  后两项在持有 gate 或 provider 屏障下用 1 秒观察窗口复现，不依赖外部网络。
- TaskControl 的关闭标记现在在维护任务 guard 的同一互斥锁内设置，锁外取消 token、通知并释放
  worker guards。直接 reload 在等待 gate 和构建候选时使用取消优先的 select，关闭返回明确的
  `AppError::Cancelled`。最终同步发布使用该生命周期锁，期间不 await；关闭先到则拒绝候选，
  发布已经进入临界区则完成后再允许关闭返回。后台注册表仍先关闭准入，再关闭 TaskControl。
- `reload-shutdown-session-before.log` 为 **0 通过 / 1 产品失败**：完整候选尚未发布，共享
  SessionManager 的 ToolExecutor 已切到候选 PluginHost。现在候选保存待应用的 providers、
  processor、executor 和 session config，仅在最终发布获准后 reconfigure。丢弃候选时释放这些
  准备资源。该 before 日志首先失败于提前重配断言，不能算成两项独立复现。
- 最终发布回归分别覆盖关闭胜出和发布胜出：前者保持原快照、会话执行器及进程 PluginHost 槽，
  并等待候选插件清理；后者在真实 tracing 更新处暂停发布，协调独立线程调用关闭，确认关闭
  等待临界区结束后观察到完整新代次。它们证明受控顺序，不代表多处读操作组成原子事务。
- `reload-shutdown-api-before.log` 复现设置编辑落盘后重载被关闭取消，却因服务错误丢失类型而
  返回 **500**。`RuntimeControlServiceError` 现在明确区分 Shutdown 与普通 Operation。
  Application、REST 设置和 Provider Studio 的多层 anyhow context 保留 Shutdown 的
  **DependencyUnavailable / 503 / Backoff / Retry** 语义；普通错误保留内部失败和有效诊断。
  实际路由及 Application set/delete 回归确认代次不变，API 不伪造完成报告；已落盘编辑不回滚。
- `reload-shutdown-managed-before.log` 为 **0 通过 / 2 产品失败**：关闭发生在后台工作的一次
  poll 内，取消分支已经被轮询，work 分支返回的取消却被记成 Failed。Runtime 的统一后台任务
  边界现在把明确的 AppError::Cancelled 投影为 Cancelled；服务适配器保留 Shutdown 类型，
  目录刷新也不会把这类取消记为刷新失败。普通错误仍是 Failed。此前在 provider hook 中关闭
  的观察用例通过，见 `reload-shutdown-managed-provider-observation.log`，不计为失败复现。
- 本轮净增 **11 项测试**：实际 Runtime 8、Application 2、API 1。Runtime callback 模块现在
  **28 项**。测试 fixture 改为仅禁用自动维护，保持 Runtime 生命周期正常；生产 new 始终启用
  自动维护。此前 fixture 通过完整 shutdown 静音维护后继续 reload，依赖了本轮修复的缺陷。
  初次 Application focused 的 1 项失败是新增断言误要求公开提示保留所有中间包装文字；
  已按现有 scrubber 的操作上下文/根因契约修正测试，没有放宽产品错误契约。
- 七组各 **20 个独立进程**重复回归：host **25**、runtime-plugins **29**、SDK **37**、
  宏 HTTP 插件 **5**、实际 Runtime **47**、实际应用 **1**、API **3**，共
  **140 个进程、2,940 次执行**；每次清单计数及退出码均匹配。
- **25 项完整检查全部通过**：工作区测试和 Clippy、8 个相关包全功能/全目标 Clippy
  （均 `-D warnings`）、上述矩阵与重复运行、fmt、**19 项失败语义约束**及 diff check。
  未新增依赖；SDK protocol 仍为 2，HTTP instance extension 仍为 1。
- `reload-shutdown-verification.json` 完成于 `2026-09-06T15:36:36.478071+00:00`。
  验证前后受跟踪及非忽略未跟踪源码 SHA-256 均为
  `2f95934d4a13dd309c09e6da59c1a51437a7dde5c0c2e6dd800c397f15f43410`；tracked diff SHA-256 均为
  `9d9fa488eda1fdb6054552b6e71c346d7ec5aa24553164e8e00b1a6140d3939a`。之后仅更新开发和审计文档，见 `reload-shutdown-post-docs.json`。

本轮没有实现整次插件回滚，也没有把 RuntimeSnapshot、SessionManager 和进程槽的独立读取合成
原子视图。该检查点定位的候选构建改写全局提供方客户端版本，已由 **2026-09-07 00:20 检查点**
通过实际请求复现，并改为显式传递不可变标识。顶层关闭通知问题已由 **2026-09-07 01:09 检查点**处理。
代次溢出、目录状态并发更新、会话持久化竞态、远端回调/资源预算和其他平台仍需继续审查。
详细边界见 `reload-shutdown-next-observations.md`。安装服务 PID **54968**、端口 **3210**
保持运行；未提交、推送或部署。整个审计保持进行中。

## 提供方客户端标识归属与插件宿主版本（2026-09-07 00:20）

- 最终工作区 **2,711 passed / 0 failed / 5 ignored**，另有 **28 项宏编译 fixture**。
  agena-runtime 默认/全功能分别 **117 / 117 passed**，提供方基础库 **104 passed**，
  提供方适配器库 **73 passed**，Application **35 passed**。
  API 默认/仅 HTTP/无默认功能分别 **29 / 26 / 1 passed**；
  SDK 默认/stdio/HTTP/全功能分别 **139 / 144 / 177 / 181 passed**，忽略项 **3 / 3 / 3 / 4**。
  PluginHost 默认/签名/WASM/全功能分别 **213 / 218 / 217 / 222 passed**；
  runtime-plugins 默认/全功能分别 **33 / 33 passed**，bundled plugins **108 passed**，
  实际 Agena 子进程 HTTP 插件回调测试 **1 passed**。
- `client-identity-before.log` 为 **0 通过 / 3 产品失败**。本地 HTTP 服务实际捕获到：候选尚未
  发布、构建失败、构建被取消时，现役模型发现请求的 User-Agent 都从 `0.111.1` 切到候选的
  `0.222.2`，但 Runtime 仍是 generation 1。这不是配置字符串检查，而是实际发出的请求。
- 删除进程全局 `ACTIVE_CLIENT_VERSIONS`、getter 和 setter。不可变 `ProviderClientIdentity`
  由 RuntimeSnapshot 持有，显式传入候选 MCP/LSP、提供方注册表、OpenAI/Anthropic/Gemini
  适配器、GitLab 嵌套适配器、AuthManager 和 ManagedCredential 刷新句柄。独立提供方使用
  明确的默认标识或调用方传入的标识，不再读取其他运行时最近写入的全局配置。
- 不只是延后全局赋值：候选注册表在发布前就使用候选版本，保留的旧注册表在新快照发布后仍
  使用旧版本。实际 HTTP 回归覆盖三家提供方的候选/旧注册表，以及两个并存 Runtime 中
  OpenAI Responses、Chat Completions、Realtime、Anthropic、Gemini 五种适配器的模型发现。
  这只证明客户端标识归属，不代表进程全局 PluginHost 槽也已支持多个运行时的完全隔离。
- Codex 的 `client_version` 查询参数与请求 User-Agent 来自同一适配器标识；Gemini 按本次
  请求模型更新生成的 User-Agent，并保留用户自定义 User-Agent。保存配置的模型发现现在
  保留同一快照，避免目标配置、网络配置和标识在一次操作中来自不同快照。
- OAuth 交换、刷新和设备登录请求显式携带调用方标识，共享 HTTP 连接池不保存代次相关的
  User-Agent。回归检查完整构造的请求，没有执行外部登录/刷新。先前对静态 Codex OAuth
  客户端冻结首个版本的推测经核实没有生产调用者：仅 GitLab 使用普通 oauth2 客户端分支。
  删除未使用的 Codex 分支属于死代码清理，不计为独立复现的生产缺陷。
- `client-identity-plugin-version-before.log` 为 **0 通过 / 1 产品失败**：实际 HTTP 插件 init 的
  `agena_version` 收到配置中的 Codex 版本 `0.111.1`，而非宿主真实版本 `0.1.2-beta.1`。
  PluginHost composition 现在传入 Agena 包构建版本；回归覆盖首次初始化和修改设置后重新
  初始化，提供方 CLI 版本变化不会改变插件收到的宿主版本。
- 本轮净增 **9 项测试**：实际 Runtime 6、提供方基础库 1、适配器库 2；Runtime callback
  模块共 **34 项**。Clippy 曾指出适配器构造参数达到 9 个、超过仓库上限 8；已引入
  AdapterBuildContext 汇集 HTTP 客户端、环境、配置路径和标识，没有添加 lint 豁免。
  中间编译修改及该 Clippy 反馈均与上述产品失败分开保留。
- 九组各 **20 个独立进程**重复回归：提供方 OAuth 请求 **1**、适配器标识 **2**、host **25**、
  runtime-plugins **29**、SDK **37**、宏 HTTP 插件 **5**、实际 Runtime **53**、实际应用 **1**、
  API **3**，共 **180 个进程、3,120 次执行**；每次清单计数与退出码均匹配。
- **27 项完整检查全部通过**：工作区测试及 Clippy、10 个相关包全功能/全目标 Clippy
  （均 `-D warnings`）、提供方独立测试、相关功能矩阵与重复运行、fmt、**19 项失败语义约束**
  和 diff check。没有新增依赖，也没有修改配置字段、SDK protocol 2 或 HTTP instance extension 1。
  Rust 构造接口现在显式接收标识；使用这些接口的调用方需传入所选标识，旧全局 setter 已移除。
- `client-identity-verification.json` 完成于 `2026-09-06T16:20:18.078072+00:00`。
  验证前后受跟踪及非忽略未跟踪源码 SHA-256 均为
  `48c46c59cb9bdd34f441fc106e408974da9a1394893e4d7afc1d88b12497f9b3`；tracked diff SHA-256 均为
  `a2066dd754d708043baf734ce836f3a2e6b2463dfb69b876bbda19954c9e48ae`。之后仅更新开发和审计文档，见 `client-identity-post-docs.json`。

客户端标识的归属不代表凭据存储被冻结，也不代表所有服务的多次读取已经原子化。重复关闭及
session.end 通知顺序由 **2026-09-07 01:09 检查点**继续复现并处理。代次溢出、整次插件回滚、
会话持久化与目录状态竞态、真实 stdio 子进程自重载、远端回调/资源预算、浏览器和其他平台
仍保持开放；此前边界保留在 `client-identity-next-observations.md`。
本轮跨入 2026-09-07，证据目录沿用审计已有的 2026-09-06 路径。安装服务 PID **54968**、
端口 **3210** 保持运行；未提交、推送或部署。整个审计继续进行。

## 关闭通知唯一归属、生命周期顺序与批次宿主（2026-09-07 01:09）

- 最终工作区 **2,717 passed / 0 failed / 5 ignored**，另有 **28 项宏编译 fixture**。
  agena-runtime 默认/全功能分别 **123 / 123 passed**，会话库 **161 passed**；
  提供方基础库/适配器库 **104 / 73 passed**，Application **35 passed**。
  API 默认/仅 HTTP/无默认功能分别 **29 / 26 / 1 passed**；
  SDK 默认/stdio/HTTP/全功能分别 **139 / 144 / 177 / 181 passed**，忽略项 **3 / 3 / 3 / 4**。
  PluginHost 默认/签名/WASM/全功能分别 **213 / 218 / 217 / 222 passed**；
  runtime-plugins 默认/全功能分别 **33 / 33 passed**，bundled plugins **108 passed**，
  实际 Agena 子进程 HTTP 插件回调测试 **1 passed**。
- `shutdown-notification-before.log` 为 **0 通过 / 2 产品失败**：第一条 HTTP session.end
  尚未完成时重复 shutdown，会向同一活动会话发送重复通知；另一回归持有实际生命周期发布锁，
  shutdown 等待该锁时插件已经收到通知，而控制状态仍未关闭。
- `shutdown-notification-admission-before.log` 为 **0 通过 / 2 产品失败**：16 个并发首次
  shutdown 各自安排广播；没有 Tokio executor 时，两次关闭诊断观察到的 stopped 值为
  `[false, true]`，而期望是关闭之后只诊断一次的 `[true]`。
- TaskControl 在发布所用的维护互斥锁内判断并设置首次关闭状态，锁外取消 token、通知及释放
  worker guards。Runtime 先关闭后台准入并完成该关闭转换，再选择通知宿主；只有首次调用者
  安排广播。已经进入同步临界区的发布仍先完成，再允许关闭及其通知继续。无 executor 时仍先
  关闭，只诊断一次；后续 shutdown 不重新尝试通知。
- `shutdown-notification-batch-before.log` 为 **0 通过 / 2 产品失败**：第一个通知等待期间
  重配 SessionManager，原宿主只收到 `[1]`，应收到的同批名单是 `[1, 2]`；已构造但尚未轮询
  的广播也选中了后来的宿主，原宿主收到空列表。这两项通过正式 reconfigure 方法控制边界，
  不等同于整次 Runtime reload 的端到端事务测试。
- `broadcast_active_session_end` 现在同步构造一个持有 PluginHost 和执行注册表的 future。
  宿主在入队前捕获，整批会话都用该宿主；异步等待期间不再反复读取 SessionManager 的执行
  ArcSwap，也不必由 future 持有整个 manager。Runtime 在已关闭状态下先构造它，再交给
  detached 调度器。活动会话名单仍在 future 开始执行时读取，没有冻结在 shutdown 调用瞬间。
- 本轮新增 **6 项实际 Runtime 测试**，callback 模块共 **40 项**。测试用内存 SQLite 创建
  真实持久化会话、提交真实执行，并在 HTTP user.prompt.submit 中暂停；尚未进入模型选择或
  任何外部提供方请求。session.end 在实际 SDK HTTP 端记录并通过屏障控制响应，测试结束时
  显式取消持有的执行。生命周期顺序用例持有真实 running_guard；此前在 tracing 更新处暂停
  实际快照发布的回归继续保留。没有新增生产测试钩子。
- 首次完整验证的实际应用独立重复第 **10** 次失败：看到 `zh-CN` 候选配置后，下一次
  SDK reload 请求返回 HostUnavailable。候选配置和新回调凭据在发布完成前可见，原测试把
  它们误当作完成条件。测试现在保留 REST 接受响应中的 task ID，经已认证的任务查询接口
  等待该任务 `Succeeded` 后才发起下一次重载；终态失败立即报告，不盲目重试重载请求。
  修正只涉及 `apps/agena/tests/http_plugin_callbacks.rs`，运行时的候选配置和准入约定保持有效。
  失败日志、源码清单及当时 fixture 均归档在 `shutdown-notification-failed-attempt-01/`。
- 九组各 **20 个独立进程**重复回归：提供方 OAuth 请求 **1**、适配器标识 **2**、host **25**、
  runtime-plugins **29**、SDK **37**、宏 HTTP 插件 **5**、实际 Runtime **59**、实际应用 **1**、
  API **3**，共 **180 个进程、3,240 次执行**；每次清单计数及退出码均匹配。
- 固定矩阵 **28 项检查均有通过证据**：工作区测试与 Clippy、11 个相关包全功能/全目标
  Clippy（均 `-D warnings`）、会话库及提供方独立测试、相关功能矩阵与重复运行、fmt、
  **19 项失败语义约束**和 diff check。复用修正前已通过且不受应用 fixture 影响的 **16 项**
  检查和 **7 组**完整重复结果；修正后执行 **12 项**检查，其中包含实际应用的全部 20 次
  重复、此前未执行的 API 重复、剩余功能组合及全工作区测试/Clippy。源码清单确认两阶段
  之间只改了上述测试文件，每条复用记录保留原源码哈希及归档日志路径；最终汇总不代表
  所有检查都在同一个源码快照上重跑。没有新增依赖、配置字段或协议版本变更；外部 shutdown
  仍是同步请求，`broadcast_active_session_end(...).await` 的调用形式继续可用。
- `shutdown-notification-verification.json` 完成于 `2026-09-06T17:09:00.795272+00:00`。
  修正后续跑的前后，受跟踪及非忽略未跟踪源码 SHA-256 均为
  `e57d934f55c95dbba0012582c03af488ba62b2cf34da471b881f23a317f7153b`；复用证据的原始源码 SHA-256 为
  `2b731477cc213d8e505fefe9e62aa5d43a36fd2a1a69067e7de390c0a7d9edc0`。两阶段 tracked diff SHA-256 均为
  `ea4cfd07ecc885eb0922980a49b71220d246369388e590426b2ce07a74d1793b`。随后仅更新开发和审计文档，见 `shutdown-notification-post-docs.json`。

本轮保证一次发起、关闭顺序和批次宿主一致性。shutdown 不等待 detached 通知全部完成，现有
每插件五秒超时也不是所有会话的总关闭期限。通用单向通知没有持久化应用确认；进程退出、远端
停止或作用域关闭仍会影响交付。保留 SessionManager 的执行准入/取消、在途 provider 工作、
通知排空与进程退出的关系、旧执行代次的通知归属及全局预算留待后续审查。代次溢出、整次插件回滚、
持久化竞态、远端回调、真实 stdio 自重载、浏览器和其他平台仍开放，见
`shutdown-notification-next-observations.md`。安装服务 PID **54968**、端口 **3210** 保持运行；
未提交、推送或部署。按用户要求，本轮收尾后停止继续展开；未完成范围保留供后续决定。

## 保持开放的审查范围

以下为后续工作，不作为已完成或已排除的问题。应继续从当前代码和运行证据推进。

1. **持久化与并发**：SQLite 事务/迁移边界、会话取消与完成竞态、缓存淘汰、
   调度器重复执行与崩溃恢复、插件卸载和后台任务资源释放。当前回归通过只证明已有覆盖，
   不等于这些路径已经逐一审计。
2. **服务端安全与接口一致性**：文件路径和符号链接边界、批量替换的部分写入语义、
   预览代理/终端访问、认证与 token 生命周期、CORS/Origin 检查及 Workbench 错误 envelope。
   `apps/agena/src/server/error.rs` 等表面仍有不同于核心 API `problem` 的错误形状，需继续对齐评估。
3. **Rust 依赖与构建策略**：`deny.toml` 仍有 RSA 例外，已核实当前不使用 RSA 私钥运算，
   仍应追踪上游修复或可缩减的 JWT 后端依赖。LRU/ChaCha20 和本机 dist 隔离问题已处理；
   各发布目标的 unwind 支持及构建 std 的策略仍需实际验证。
4. **平台和功能组合**：WASM/签名功能已有 macOS 行为测试；继续验证运行时的功能转发、
   Linux/Windows 和 universal-target 发布矩阵。本地 macOS 成功不能替代这些结果。
5. **Web 运行体验**：实际浏览器检查 Markdown/Mermaid/Monaco、移动布局、断网重连、
   长会话和大文件的性能/内存行为。当前 Browser 不可用只影响该项，不阻止其他审查。
6. **长期维护**：继续检查文档、旧接口残留、大模块职责边界和测试对真实行为的覆盖质量。
   当前很多 Web 测试属于源码契约检查，需要针对重要交互补充行为证据，而非仅增加快照数量。

后续代码审查已定位的优先路径（尚未作为修复完成项）：

- 上传/写入的路由上限、编码膨胀及 body/JSON 错误 envelope 已按上一节修复。其余 Workbench
  提取器仍需一致性审查；慢上传的超时、排队公平性和完整缓冲的内存成本保持开放。
- upload/write/replace 的单文件失败保留、替换生成上限、revision 和批量部分完成报告已按
  上述范围修复；外部写入的最终比较/发布竞态、运行时其他文件工具、跨文件事务及取消后实际
  完成状态仍需处理。普通 read/raw/download 已限制实际读取字节，仍需审查已完成响应/排队
  的总内存预算、慢文件系统期限和外部原地写入的快照语义。路径的真实符号链接
  边界以及检查与实际打开之间的竞态仍保持开放。
- 调度器的持久化失败传递、完整状态比较、过期占用恢复、续租和历史事务已经按上述范围修复。
  下一步继续检查 sink 与 session 持久化交接的崩溃窗口、大量历史/终态任务的查询成本、
  损坏行的隔离和可修复诊断，以及系统时钟跳变和跨平台行为。SQLite 失败记录和 schema 初始化
  已按上述范围修复；完整事务/触发器审查及会话取消/完成竞态仍未完成。
- 通用 SQLite 辅助函数的原错误保留、busy 扩展码和退避起点已按上一节修复；独立 engine/
  model-catalog 包装的复合错误类型策略仍需评估。parts 内容/控制字段与时间戳的 UPDATE
  约束、两个后端的候选更新准备及完成返回值已按上一节修复。完整状态转换图、冲突终态完成、
  其他内存后端跨锁/批量/导入路径、完整失败类型读取、既有坏行和提交后 effect 的取消语义
  继续保持开放。
- stdio 的重新初始化、manifest/protocol 校验、宿主绑定及失效候选恢复已按上述范围修复。
  下一步检查多个插件的整体回滚语义，以及重载、关闭和服务依赖失败同时发生的更多顺序。
  当前构建取消保护不能替代原子重载设计。
- 全局工具/display/theme 的跨所有者替换、精确清理、显式删除及进程快照归属，以及
  ScopedRegistry 的准入/发布、失败保留和代次边界已按上述范围修复。hook/transport
  登记、回退清理在 current-scope 检查后的 await 竞态和首次作用域并发创建也已按
  14:32 检查点处理；ScopedHostClient 的准入、lease、取消和原 owner 保留由 14:48
  检查点处理。本地目录方法的 authority 校验及已签发/已验证上下文的隔离由 15:11 检查点
  处理。继续审查 HTTP 与其他实际回调入口，以及跨注册表工具目录版本/事件时序。正常使用
  及连续重启的作用域元数据/effect 记录增长、自定义异步清理器长期阻塞、在途通知持有宿主
  的引用关系仍未完成审查。
- HTTP 的精确 callback scope、凭据交接及工具/会话/display/theme 转交由 17:23 检查点
  在显式配置的宿主和正式 bridge 上验证。19:28 检查点进一步验证了本机 HTTP
  生产 composition 的初次监听、UI 密码下启动和候选配置边界；跨机器回调地址、旧公共
  callback 路由与整体服务发布原子性仍需处理。插件自重载的请求/状态流程由 20:40
  检查点验证；22:28 检查点补齐后台准入/关闭和共享活动数量上限，23:36 检查点补齐
  直接/在途重载的关闭与发布顺序及候选会话重配。2026-09-07 00:20 检查点完成提供方客户端
  标识隔离和插件宿主版本修复。2026-09-07 01:09 检查点处理关闭通知的唯一发起、生命周期
  顺序和批次宿主。通知排空/总期限、关闭后的会话执行准入及旧执行代次交付仍需独立证据。
  跨发起调用公平性、独立外部重载、多处状态的原子读取、
  凭据存储并发及多个运行时共享的进程 PluginHost 槽仍有边界。相同外部实例在
  配置变化后重新 init 的 shutdown 所有权由 18:07 检查点处理；插件对象重建及宏生成
  PluginSettings 的 OnceLock 重复初始化由 18:45 检查点处理。整个重载失败后的恢复、
  工厂自行共享的可变状态、分离任务与阻塞构造/析构仍需继续审查。
  cdylib/WASM 的其他保留状态转交和初次
  stdio 构造从通用 handler 到进程 handler 的窗口仍需独立证据。返回资源及分离后台任务
  的生命周期也需单独审查。
  SDK HTTP 发送侧上下文由 15:36 检查点修复，宿主 stream authority 与调用 ID 绑定、
  pending 清理和终态保留由 16:01 检查点处理。17:23 的弱谱系路由支持 bridge 按凭据找到
  候选 HostHandle；19:28 已补 AgenaRuntime 候选配置/发布和实际应用启动证据，
  20:40 已验证活动工具、流、默认上下文 hook 与嵌套 settings 的自重载；完整回滚、
  单向通知及实际 stdio 子进程自重载仍需独立证据。
- 继续检查进程最后 stderr 的排空、流队列的总字节预算、文件摘要校验与 OS 实际打开
  执行文件之间的竞态，以及 Windows 的查找与启动行为。

完成条件：以上范围有对应的当前状态证据；确认的问题得到修复或有明确的剩余约束；
适用的本地/远端/平台验证实际执行通过。不得仅凭本轮通过的测试数量宣布全部技术债已消除。
